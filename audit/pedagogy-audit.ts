// Backward-compatible entry point. The V2.10 curriculum audit evaluates the
// actual ten-stage Learning Engine, assessment contracts, projects, and
// progression instead of scoring only the legacy lesson block count.
await import('./curriculum-audit')
