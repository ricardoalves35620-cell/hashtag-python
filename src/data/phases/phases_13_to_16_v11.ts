import type { Bilingual, CodeRequirement, Exercise, LessonBlock, Phase, TestCase } from '../types'

const b = (en: string, pt: string): Bilingual => ({ en, pt })
const heading = (en: string, pt: string): LessonBlock => ({ type: 'heading', content: b(en, pt) })
const text = (en: string, pt: string): LessonBlock => ({ type: 'text', content: b(en, pt) })
const code = (en: string, pt: string): LessonBlock => ({ type: 'code', code: b(en, pt), language: 'python' })
const warning = (en: string, pt: string): LessonBlock => ({ type: 'warning', content: b(en, pt) })
const tip = (en: string, pt: string): LessonBlock => ({ type: 'tip', content: b(en, pt) })

function exactTest(
  id: string,
  description: Bilingual,
  afterCode: string,
  expected: Bilingual,
  points: number,
  requirements: CodeRequirement[],
  hidden = false,
): TestCase {
  return {
    id,
    description,
    expectedOutput: expected,
    inputs: [],
    afterCode,
    checks: [
      { type: 'no_error' },
      { type: 'equals_any', value: Array.from(new Set([expected.en, expected.pt])), target: 'test_output', textMode: 'normalized' },
    ],
    points,
    hidden,
    codeRequirements: requirements,
  }
}

function exercise(
  id: string,
  title: Bilingual,
  description: Bilingual,
  starterCode: string,
  sampleOutput: Bilingual,
  hints: Bilingual[],
  requirements: CodeRequirement[],
  publicTest: TestCase,
  hiddenTest: TestCase,
  difficulty: Exercise['difficulty'],
  objective: Bilingual,
  workplaceContext: Bilingual,
  successCriteria: { en: string[]; pt: string[] },
  commonMistakes: { en: string[]; pt: string[] },
): Exercise {
  return {
    id, title, description, starterCode, hints, sampleOutput,
    grading: { tests: [publicTest, hiddenTest], codeRequirements: requirements, timeoutMs: 3000 },
    objective, difficulty,
    estimatedMinutes: difficulty === 'guided' ? 12 : difficulty === 'challenge' ? 30 : 20,
    skillIds: [], successCriteria, commonMistakes, workplaceContext,
  }
}

export const phase13: Phase = {
  id: 13,
  title: b("Creating Functions", "Criando Funções"),
  description: b("Package repeated decisions into named, testable units with explicit inputs and returns.", "Empacote decisões repetidas em unidades nomeadas e testáveis, com entradas e retornos explícitos."),
  icon: "🔧",
  libraries: [],
  track: 'core',
  stage: 'base',
  estimatedHours: 7,
  lesson: {
    title: b("Functions: reusable behavior with a clear contract", "Funções: comportamento reutilizável com contrato claro"),
    blocks: [
      heading("🌍 World hook — every production system is assembled from functions", "🌍 Gancho do mundo real — todo sistema de produção é montado com funções"),
      text("A checkout system calculates totals, a logistics app estimates delivery time, a school portal converts grades and a game updates player health. Professional software does not keep these rules as loose lines scattered across screens. Each rule becomes a named function that can be called, tested and changed in one place. Functions are the first tool that lets a beginner move from “a script that works once” to software with reusable parts and predictable boundaries.", "Um checkout calcula totais, um app de logística estima o prazo, um portal escolar converte notas e um jogo atualiza a vida do jogador. Software profissional não mantém essas regras como linhas soltas espalhadas por telas. Cada regra vira uma função nomeada que pode ser chamada, testada e alterada em um só lugar. Funções são a primeira ferramenta que permite ao iniciante sair de “um script que funciona uma vez” para software com partes reutilizáveis e limites previsíveis."),
      text("A function has four responsibilities: receive information, validate assumptions, perform one coherent job and return a useful result. Not every function needs every step, but every function should have a reason to exist. A good name describes the action and the returned value: calculate_total, normalize_email and is_eligible communicate more than process_data or do_it. Naming is part of the contract because callers read the name before they read the implementation.", "Uma função possui quatro responsabilidades: receber informações, validar premissas, realizar um trabalho coerente e retornar um resultado útil. Nem toda função precisa de cada etapa, mas toda função deve ter uma razão para existir. Um bom nome descreve a ação e o valor retornado: calcular_total, normalizar_email e esta_elegivel comunicam mais que processar_dados ou fazer_isso. O nome faz parte do contrato porque quem chama lê o nome antes de ler a implementação."),
      heading("🧩 Physical analogy — a labeled machine on a workbench", "🧩 Analogia física — uma máquina identificada na bancada"),
      text("Imagine a small machine labeled “cut cable”. You feed it a requested length, it performs one operation and gives you a cut piece. The label is the function name, the opening is the parameter list, the internal mechanism is the indented body and the finished piece is the return value. You can use the same machine for 2 meters, 5 meters or 10 meters because the changing information enters as an argument.", "Imagine uma pequena máquina com o rótulo “cortar cabo”. Você fornece o comprimento desejado, ela realiza uma operação e entrega o pedaço cortado. O rótulo é o nome da função, a abertura é a lista de parâmetros, o mecanismo interno é o corpo indentado e a peça pronta é o valor de retorno. A mesma máquina serve para 2, 5 ou 10 metros porque a informação variável entra como argumento."),
      text("The analogy also explains separation of concerns. A cable-cutting machine should not invoice the customer, update inventory and send an email at the same time. Those are other machines coordinated by a higher-level workflow. Small functions are easier to verify because each one has fewer reasons to fail. Later you will connect them into larger services without losing the ability to test each part independently.", "A analogia também explica separação de responsabilidades. Uma máquina de cortar cabo não deveria faturar o cliente, atualizar estoque e enviar e-mail ao mesmo tempo. Essas são outras máquinas coordenadas por um fluxo de nível mais alto. Funções pequenas são mais fáceis de verificar porque possuem menos motivos para falhar. Depois você vai conectá-las em serviços maiores sem perder a capacidade de testar cada parte separadamente."),
      heading("🐍 Fundamentals 1 — define, indent and call", "🐍 Fundamentos 1 — defina, indente e chame"),
      code("def show_welcome():\n    print(\"Welcome to the study room\")\n    print(\"Choose one mission\")\n\nshow_welcome()\nshow_welcome()  # the same behavior is reused", "def mostrar_boas_vindas():\n    print(\"Bem-vindo à sala de estudos\")\n    print(\"Escolha uma missão\")\n\nmostrar_boas_vindas()\nmostrar_boas_vindas()  # o mesmo comportamento é reutilizado"),
      text("def creates the function object but does not run its body. The indented lines run only when the call expression uses parentheses. Forgetting the parentheses gives you the function itself rather than its result. A function may be called before or after other calls, but its definition must have executed before the first call reaches that line.", "def cria o objeto função, mas não executa seu corpo. As linhas indentadas só rodam quando a chamada usa parênteses. Esquecer os parênteses fornece a própria função em vez do resultado. Uma função pode ser chamada antes ou depois de outras chamadas, mas sua definição precisa ter sido executada antes de a primeira chamada alcançar aquela linha."),
      heading("🐍 Fundamentals 2 — parameters and arguments", "🐍 Fundamentos 2 — parâmetros e argumentos"),
      code("def format_score(player, points):\n    label = player.strip().title()\n    return f\"{label}: {points} pts\"\n\nprint(format_score(\"  ana \", 120))\nprint(format_score(\"leo\", 95))", "def formatar_pontuacao(jogador, pontos):\n    rotulo = jogador.strip().title()\n    return f\"{rotulo}: {pontos} pts\"\n\nprint(formatar_pontuacao(\"  ana \", 120))\nprint(formatar_pontuacao(\"leo\", 95))"),
      text("player and points are parameters: local names written in the definition. “ana” and 120 are arguments: concrete values supplied by the caller. During the call Python binds each argument to its parameter, creates a local function scope and runs the body. The next call receives a fresh local scope, so previous values do not leak into the next player.", "player e points são parâmetros: nomes locais escritos na definição. “ana” e 120 são argumentos: valores concretos fornecidos por quem chama. Durante a chamada, Python liga cada argumento ao parâmetro, cria um escopo local e executa o corpo. A próxima chamada recebe um novo escopo local, então valores anteriores não vazam para o próximo jogador."),
      heading("🐍 Fundamentals 3 — return data instead of trapping it inside print", "🐍 Fundamentos 3 — retorne dados em vez de prendê-los no print"),
      code("def discounted_price(price, percent):\n    if price < 0 or percent < 0:\n        raise ValueError(\"negative value\")\n    discount = price * percent / 100\n    return round(price - discount, 2)\n\nfinal_price = discounted_price(80, 15)\nprint(final_price)\nprint(final_price + 5)  # caller can reuse the number", "def preco_com_desconto(preco, percentual):\n    if preco < 0 or percentual < 0:\n        raise ValueError(\"negative value\")\n    desconto = preco * percentual / 100\n    return round(preco - desconto, 2)\n\npreco_final = preco_com_desconto(80, 15)\nprint(preco_final)\nprint(preco_final + 5)  # quem chamou pode reutilizar o número"),
      text("print sends text to the console for a person. return sends a Python value back to the caller. A returned number can be stored, compared, added or passed into another function. A printed number is only display output. When a function reaches return, it stops immediately; statements after that return in the same path do not run. A function with no reached return produces None.", "print envia texto ao console para uma pessoa. return envia um valor Python de volta para quem chamou. Um número retornado pode ser armazenado, comparado, somado ou passado para outra função. Um número impresso é apenas saída visual. Quando a função alcança return, ela para imediatamente; instruções depois desse return no mesmo caminho não executam. Uma função sem return alcançado produz None."),
      heading("🐍 Fundamentals 4 — one job, explicit validation", "🐍 Fundamentos 4 — um trabalho, validação explícita"),
      code("def minutes_to_seconds(minutes):\n    if not isinstance(minutes, (int, float)):\n        raise TypeError(\"minutes must be numeric\")\n    if minutes < 0:\n        raise ValueError(\"minutes cannot be negative\")\n    return minutes * 60\n\nprint(minutes_to_seconds(2.5))", "def minutos_para_segundos(minutos):\n    if not isinstance(minutos, (int, float)):\n        raise TypeError(\"minutes must be numeric\")\n    if minutos < 0:\n        raise ValueError(\"minutes cannot be negative\")\n    return minutos * 60\n\nprint(minutos_para_segundos(2.5))"),
      text("Validation belongs close to the boundary where data enters. This prevents an invalid value from travelling through many calculations and failing somewhere unrelated. The error message should identify the violated rule, not merely say “error”. Clear exceptions also make hidden tests and future debugging more useful because the failure explains which contract was broken.", "A validação pertence perto da fronteira onde os dados entram. Isso impede que um valor inválido percorra muitos cálculos e falhe em um lugar sem relação aparente. A mensagem deve identificar a regra violada, não apenas dizer “erro”. Exceções claras também tornam testes ocultos e depuração futura mais úteis porque a falha explica qual contrato foi quebrado."),
      heading("🏗️ Real scenario 1 — restaurant preparation estimate", "🏗️ Cenário real 1 — estimativa de preparo no restaurante"),
      text("A restaurant receives dishes with quantity and minutes per portion. The kitchen needs one function that calculates total preparation minutes and another that assigns a queue label. Keeping calculation separate from presentation allows the same rule to serve the cashier screen, the kitchen display and a nightly report.", "Um restaurante recebe pratos com quantidade e minutos por porção. A cozinha precisa de uma função que calcule minutos totais de preparo e outra que atribua um rótulo de fila. Separar cálculo de apresentação permite que a mesma regra sirva à tela do caixa, ao painel da cozinha e ao relatório noturno."),
      code("def preparation_minutes(quantity, minutes_each):\n    if quantity < 0 or minutes_each < 0:\n        raise ValueError(\"negative preparation value\")\n    return quantity * minutes_each\n\ndef queue_label(total_minutes):\n    if total_minutes > 45:\n        return \"priority\"\n    if total_minutes > 20:\n        return \"standard\"\n    return \"quick\"\n\nminutes = preparation_minutes(8, 6)\nprint(minutes, queue_label(minutes))", "def minutos_preparo(quantidade, minutos_por_item):\n    if quantidade < 0 or minutos_por_item < 0:\n        raise ValueError(\"negative preparation value\")\n    return quantidade * minutos_por_item\n\ndef rotulo_fila(total_minutos):\n    if total_minutos > 45:\n        return \"priority\"\n    if total_minutos > 20:\n        return \"standard\"\n    return \"quick\"\n\nminutos = minutos_preparo(8, 6)\nprint(minutos, rotulo_fila(minutos))"),
      text("Notice that queue_label does not know quantities or recipes. It receives the already calculated total and makes one decision. This makes thresholds easy to test with boundary values such as 20, 21, 45 and 46. Professional tests focus on those boundaries because that is where comparison mistakes usually hide.", "Observe que queue_label não conhece quantidades nem receitas. Ela recebe o total já calculado e toma uma decisão. Isso facilita testar limites como 20, 21, 45 e 46. Testes profissionais focam nesses limites porque é onde erros de comparação costumam se esconder."),
      heading("🏗️ Real scenario 2 — science sample conversion", "🏗️ Cenário real 2 — conversão de amostra científica"),
      text("A small lab records temperature in Celsius but exports Fahrenheit for another instrument. A pure conversion function is ideal: the same input always produces the same output and the function changes no external data. Pure functions are easier to trust, cache and test because their behavior depends only on their arguments.", "Um pequeno laboratório registra temperatura em Celsius, mas exporta Fahrenheit para outro instrumento. Uma função pura de conversão é ideal: a mesma entrada sempre produz a mesma saída e a função não altera dados externos. Funções puras são mais fáceis de confiar, armazenar em cache e testar porque seu comportamento depende apenas dos argumentos."),
      code("def celsius_to_fahrenheit(celsius):\n    if celsius < -273.15:\n        raise ValueError(\"below absolute zero\")\n    return round(celsius * 9 / 5 + 32, 2)\n\nreadings = [0, 20, -10]\nconverted = [celsius_to_fahrenheit(value) for value in readings]\nprint(converted)", "def celsius_para_fahrenheit(celsius):\n    if celsius < -273.15:\n        raise ValueError(\"below absolute zero\")\n    return round(celsius * 9 / 5 + 32, 2)\n\nleituras = [0, 20, -10]\nconvertidas = [celsius_para_fahrenheit(valor) for valor in leituras]\nprint(convertidas)"),
      heading("⚠️ Common errors — read the Python message, then fix the contract", "⚠️ Erros comuns — leia a mensagem do Python e corrija o contrato"),
      warning("TypeError: function() missing 1 required positional argument appears when the call supplies fewer arguments than the definition requires. TypeError: function() takes 2 positional arguments but 3 were given means the caller supplied too many. NameError often means the function name was misspelled or the definition did not run. IndentationError means part of the body is not aligned. A silent None result means no return was reached. Fix the definition or call instead of printing a hardcoded expected answer.", "TypeError: function() missing 1 required positional argument aparece quando a chamada fornece menos argumentos que a definição exige. TypeError: function() takes 2 positional arguments but 3 were given significa argumentos demais. NameError normalmente indica nome digitado errado ou definição não executada. IndentationError significa corpo desalinhado. Um resultado None silencioso indica que nenhum return foi alcançado. Corrija a definição ou a chamada em vez de imprimir uma resposta esperada fixa."),
      text("Another frequent error is mixing calculation and display. A function prints a beautifully formatted total but returns None, then a later line tries to add tax to that None. Separate the numeric function from the presentation function. First calculate and return the number; then let the caller decide whether to print, save, compare or send it.", "Outro erro frequente é misturar cálculo e exibição. Uma função imprime um total bonito, mas retorna None; depois outra linha tenta somar imposto a esse None. Separe a função numérica da apresentação. Primeiro calcule e retorne o número; depois deixe quem chamou decidir se imprime, salva, compara ou envia."),
      heading("💡 Pro tip — design from the caller backward", "💡 Dica pro — projete a partir de quem chama"),
      tip("Before writing the body, write one realistic call and the expected returned value. That example reveals the function name, parameters and output type. Then implement the smallest body that satisfies the contract and test an empty, negative, accented or very large case when relevant. Designing from the caller prevents functions that are technically correct but awkward to reuse.", "Antes de escrever o corpo, escreva uma chamada realista e o valor retornado esperado. Esse exemplo revela nome, parâmetros e tipo de saída. Depois implemente o menor corpo que atende ao contrato e teste caso vazio, negativo, acentuado ou muito grande quando fizer sentido. Projetar a partir de quem chama evita funções tecnicamente corretas, mas difíceis de reutilizar."),
      heading("📋 Recap and bridge to richer parameters", "📋 Resumo e ponte para parâmetros mais ricos"),
      text("You can now define and call functions, distinguish parameters from arguments, return reusable values, validate input and split a workflow into small responsibilities. The next phase makes calls more expressive with positional arguments, keyword arguments, defaults and keyword-only options. Those tools are powerful only when the basic contract learned here remains clear.", "Agora você consegue definir e chamar funções, distinguir parâmetros de argumentos, retornar valores reutilizáveis, validar entradas e dividir um fluxo em pequenas responsabilidades. A próxima fase torna chamadas mais expressivas com argumentos posicionais, nomeados, padrões e opções apenas nomeadas. Essas ferramentas só são poderosas quando o contrato básico aprendido aqui continua claro."),
    ],
  },
  exercises: [
    exercise(
      "p13-guided-label",
      b("🟢 Guided — create a reusable label function", "🟢 Guiado — crie uma função reutilizável de rótulo"),
      b("Complete clean_label so it strips surrounding spaces, converts the text to title case and returns the result.", "Complete clean_label para remover espaços externos, converter o texto para formato de título e retornar o resultado."),
      "def clean_label(label):\n    cleaned = label.___().___()\n    return ___",
      b("São Paulo", "São Paulo"),
      [b("Call strip before title.", "Chame strip antes de title."), b("Return the local variable rather than printing it.", "Retorne a variável local em vez de imprimi-la."), b("Use the parameter; do not hardcode the visible label.", "Use o parâmetro; não fixe o rótulo visível.")],
      [{ kind: "function", value: "clean_label" }, { kind: "call", value: "strip" }, { kind: "call", value: "title" }, { kind: "node", value: "Return" }],
      exactTest("p13-label-visible", b("Normalizes a visible label", "Normaliza um rótulo visível"), "print(clean_label(\"  north zone \"))", b("North Zone", "North Zone"), 60, [{ kind: "function", value: "clean_label" }, { kind: "call", value: "strip" }, { kind: "node", value: "Return" }], false),
      exactTest("p13-label-accented", b("Preserves accented text", "Preserva texto acentuado"), "print(clean_label(\"  são paulo \"))", b("São Paulo", "São Paulo"), 40, [{ kind: "function", value: "clean_label" }, { kind: "call", value: "title" }, { kind: "node", value: "Return" }], true),
      "guided",
      b("Practice the full define-call-return cycle.", "Praticar o ciclo completo definir-chamar-retornar."),
      b("Normalizing labels imported from forms and spreadsheets.", "Normalização de rótulos importados de formulários e planilhas."),
      { en: ["Returns a string", "Removes outer spaces", "Preserves accents"], pt: ["Retorna string", "Remove espaços externos", "Preserva acentos"] },
      { en: ["Printing instead of returning", "Using a fixed string", "Calling title on the function"], pt: ["Imprimir em vez de retornar", "Usar string fixa", "Chamar title na função"] },
    ),
    exercise(
      "p13-complete-total",
      b("🟡 Complete — calculate an order total", "🟡 Complete — calcule o total de um pedido"),
      b("Finish order_total(prices). Reject a negative price and return the rounded sum.", "Termine order_total(prices). Rejeite preço negativo e retorne a soma arredondada."),
      "def order_total(prices):\n    if any(price < 0 for price in prices):\n        raise ValueError(\"negative price\")\n    total = ___\n    return ___",
      b("35.5", "35.5"),
      [b("sum(prices) calculates the total.", "sum(prices) calcula o total."), b("Use round(total, 2).", "Use round(total, 2)."), b("An empty list should return 0.", "Uma lista vazia deve retornar 0.")],
      [{ kind: "function", value: "order_total" }, { kind: "call", value: "sum" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p13-total-visible", b("Adds normal prices", "Soma preços normais"), "print(order_total([10, 20.5, 5]))", b("35.5", "35.5"), 60, [{ kind: "function", value: "order_total" }, { kind: "call", value: "sum" }, { kind: "node", value: "Return" }], false),
      exactTest("p13-total-empty", b("Handles an empty order", "Trata pedido vazio"), "print(order_total([]))", b("0", "0"), 40, [{ kind: "function", value: "order_total" }, { kind: "call", value: "sum" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Return numeric data that another function can reuse.", "Retornar dado numérico que outra função pode reutilizar."),
      b("Calculating carts, invoices and expense batches.", "Cálculo de carrinhos, faturas e lotes de despesas."),
      { en: ["Rejects negative prices", "Returns 0 for empty input", "Rounds to two decimals"], pt: ["Rejeita preços negativos", "Retorna 0 para entrada vazia", "Arredonda em duas casas"] },
      { en: ["Printing the sum", "Returning inside a loop", "Changing the input list"], pt: ["Imprimir a soma", "Retornar dentro de loop", "Alterar lista de entrada"] },
    ),
    exercise(
      "p13-zero-status",
      b("🔴 From scratch — classify a sensor reading", "🔴 Do zero — classifique uma leitura de sensor"),
      b("Write reading_status(value). Return \"low\" below 0, \"normal\" from 0 through 100 and \"high\" above 100.", "Escreva reading_status(value). Retorne \"low\" abaixo de 0, \"normal\" de 0 até 100 e \"high\" acima de 100."),
      "def reading_status(value):\n    pass",
      b("normal", "normal"),
      [b("Use if, elif or early returns.", "Use if, elif ou retornos antecipados."), b("Test boundaries -1, 0, 100 and 101.", "Teste os limites -1, 0, 100 e 101."), b("Every path must return a string.", "Todo caminho precisa retornar uma string.")],
      [{ kind: "function", value: "reading_status" }, { kind: "node", value: "If" }, { kind: "node", value: "Return", minCount: 3 }],
      exactTest("p13-status-visible", b("Classifies a normal reading", "Classifica leitura normal"), "print(reading_status(75))", b("normal", "normal"), 60, [{ kind: "function", value: "reading_status" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], false),
      exactTest("p13-status-negative", b("Classifies a negative reading", "Classifica leitura negativa"), "print(reading_status(-5))", b("low", "low"), 40, [{ kind: "function", value: "reading_status" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Build a function with complete decision paths.", "Construir função com caminhos de decisão completos."),
      b("Lab dashboards, warehouse sensors and quality checks.", "Painéis de laboratório, sensores de estoque e controle de qualidade."),
      { en: ["Returns for every range", "Handles boundaries", "Uses the parameter"], pt: ["Retorna para todas as faixas", "Trata limites", "Usa o parâmetro"] },
      { en: ["Leaving one path without return", "Using > where >= is needed", "Printing fixed labels"], pt: ["Deixar caminho sem return", "Usar > quando precisa >=", "Imprimir rótulos fixos"] },
    ),
    exercise(
      "p13-transfer",
      b("🟣 Challenge — compose two functions", "🟣 Desafio — componha duas funções"),
      b("Write net_hours(entries) and workload_label(hours). net_hours receives [planned, blocked] pairs, rejects negative values and returns total planned minus blocked. workload_label returns \"light\", \"balanced\" or \"heavy\" for totals below 20, through 40, or above 40.", "Escreva net_hours(entries) e workload_label(hours). net_hours recebe pares [planejado, bloqueado], rejeita negativos e retorna o total planejado menos o bloqueado. workload_label retorna \"light\", \"balanced\" ou \"heavy\" para totais abaixo de 20, até 40 ou acima de 40."),
      "def net_hours(entries):\n    pass\n\ndef workload_label(hours):\n    pass",
      b("36\nbalanced", "36\nbalanced"),
      [b("Validate every planned and blocked value first.", "Valide primeiro cada valor planejado e bloqueado."), b("Accumulate planned - blocked.", "Acumule planejado - bloqueado."), b("Call workload_label with the returned total.", "Chame workload_label com o total retornado.")],
      [{ kind: "function", value: "net_hours" }, { kind: "function", value: "workload_label" }, { kind: "node", value: "For" }, { kind: "node", value: "If" }, { kind: "node", value: "Return", minCount: 2 }],
      exactTest("p13-report-visible", b("Composes a workload result", "Compõe resultado de carga"), "hours = net_hours([[20, 2], [10, 1], [12, 3]])\nprint(hours)\nprint(workload_label(hours))", b("36\nbalanced", "36\nbalanced"), 60, [{ kind: "function", value: "net_hours" }, { kind: "function", value: "workload_label" }, { kind: "node", value: "For" }, { kind: "node", value: "Return" }], false),
      exactTest("p13-report-large", b("Handles a large workload", "Trata carga grande"), "hours = net_hours([[1000000, 10]])\nprint(hours)\nprint(workload_label(hours))", b("999990\nheavy", "999990\nheavy"), 40, [{ kind: "function", value: "net_hours" }, { kind: "function", value: "workload_label" }, { kind: "node", value: "For" }, { kind: "node", value: "Return" }], true),
      "challenge",
      b("Connect small functions without hiding intermediate data.", "Conectar funções pequenas sem esconder dados intermediários."),
      b("Capacity planning for teams, kitchens and field operations.", "Planejamento de capacidade de equipes, cozinhas e operações de campo."),
      { en: ["Two focused functions", "Validation before calculation", "Correct boundary labels"], pt: ["Duas funções focadas", "Validação antes do cálculo", "Rótulos corretos nos limites"] },
      { en: ["One giant function", "Using global totals", "Returning before all rows"], pt: ["Uma função gigante", "Usar totais globais", "Retornar antes de todas as linhas"] },
    ),
  ],
  quiz: [
    { id: "q13-v11-1", question: b("What happens when Python executes a def statement?", "O que acontece quando Python executa uma instrução def?"), options: [b("It calls the function immediately", "Ela chama a função imediatamente"), b("It creates the function and binds its name", "Ela cria a função e liga seu nome"), b("It prints the body", "Ela imprime o corpo"), b("It imports a module", "Ela importa um módulo")], correctIndex: 1, explanation: b("def creates the function object. The body runs later when a call uses parentheses.", "def cria o objeto função. O corpo roda depois quando uma chamada usa parênteses.") },
    { id: "q13-v11-2", question: b("Why return a number instead of only printing it?", "Por que retornar um número em vez de apenas imprimi-lo?"), options: [b("Return makes the number reusable by the caller", "Return torna o número reutilizável por quem chamou"), b("Return always converts it to text", "Return sempre o converte em texto"), b("Print is slower in every case", "Print é mais lento em todo caso"), b("There is no difference", "Não existe diferença")], correctIndex: 0, explanation: b("Returned data can be stored, compared or passed to another function. Printed text is display output.", "Dados retornados podem ser armazenados, comparados ou passados a outra função. Texto impresso é saída visual.") },
    { id: "q13-v11-3", question: b("A function reaches return. What happens next?", "Uma função alcança return. O que acontece depois?"), options: [b("The function restarts", "A função reinicia"), b("Only the next line is skipped", "Apenas a próxima linha é pulada"), b("The function stops and sends the value back", "A função para e envia o valor de volta"), b("All local variables become global", "Todas as variáveis locais ficam globais")], correctIndex: 2, explanation: b("return ends the current function call immediately and supplies its value to the caller.", "return encerra imediatamente a chamada atual e fornece seu valor a quem chamou.") },
    { id: "q13-v11-4", question: b("Which name best communicates a focused function?", "Qual nome comunica melhor uma função focada?"), options: [b("do_stuff", "do_stuff"), b("x", "x"), b("process_everything", "process_everything"), b("calculate_shipping_cost", "calculate_shipping_cost")], correctIndex: 3, explanation: b("A verb plus a specific result reveals the function contract before the implementation is read.", "Um verbo mais resultado específico revela o contrato antes de ler a implementação.") },
  ],
  exam: {
    title: b("Daily sales function toolkit", "Kit de funções para vendas diárias"),
    scenario: b("Build two reusable functions. sales_total validates a list of numeric amounts and returns the rounded sum. performance_label classifies the total as \"low\" below 500, \"target\" through 2000, or \"high\" above 2000.", "Crie duas funções reutilizáveis. sales_total valida uma lista de valores numéricos e retorna a soma arredondada. performance_label classifica o total como \"low\" abaixo de 500, \"target\" até 2000 ou \"high\" acima de 2000."),
    requirements: { en: ["Define sales_total(amounts)", "Reject a negative amount with ValueError(\"negative amount\")", "Define performance_label(total)", "Return values instead of printing inside the functions", "Use no global state"], pt: ["Defina sales_total(amounts)", "Rejeite valor negativo com ValueError(\"negative amount\")", "Defina performance_label(total)", "Retorne valores em vez de imprimir dentro das funções", "Não use estado global"] },
    starterCode: "def sales_total(amounts):\n    pass\n\ndef performance_label(total):\n    pass",
    expectedOutput: b("750.5\ntarget", "750.5\ntarget"),
    testCases: [
      exactTest("p13-exam-visible", b("Calculates and labels a normal day", "Calcula e classifica um dia normal"), "total = sales_total([100, 250.5, 400])\nprint(total)\nprint(performance_label(total))", b("750.5\ntarget", "750.5\ntarget"), 60, [{ kind: "function", value: "sales_total" }, { kind: "function", value: "performance_label" }, { kind: "call", value: "sum" }, { kind: "node", value: "Return", minCount: 2 }], false),
      exactTest("p13-exam-empty", b("Handles an empty sales day", "Trata um dia de vendas vazio"), "total = sales_total([])\nprint(total)\nprint(performance_label(total))", b("0\nlow", "0\nlow"), 20, [{ kind: "function", value: "sales_total" }, { kind: "function", value: "performance_label" }, { kind: "node", value: "Return", minCount: 2 }], true),
      exactTest("p13-exam-hidden-hardening", b("Rejects a negative amount", "Rejeita valor negativo"), "try:\n    sales_total([10, -1])\nexcept ValueError as error:\n    print(error)", b("negative amount", "negative amount"), 20, [{ kind: "function", value: "sales_total" }, { kind: "node", value: "Raise" }], true),
    ],
  },
}

export const phase14: Phase = {
  id: 14,
  title: b("Function Parameters", "Parâmetros de Função"),
  description: b("Design flexible calls with positional, keyword, default and keyword-only parameters.", "Projete chamadas flexíveis com parâmetros posicionais, nomeados, padrão e apenas nomeados."),
  icon: "🎛️",
  libraries: [],
  track: 'core',
  stage: 'base',
  estimatedHours: 7,
  lesson: {
    title: b("Parameters: flexible inputs without ambiguous calls", "Parâmetros: entradas flexíveis sem chamadas ambíguas"),
    blocks: [
      heading("🌍 World hook — parameters are the control panel of every API", "🌍 Gancho do mundo real — parâmetros são o painel de controle de toda API"),
      text("When an app requests a report, sends a notification or searches a catalog, it calls functions with options. Some inputs are essential, such as a customer id. Others have sensible defaults, such as page size or language. Parameters turn a fixed routine into a reusable service, but too many unclear parameters create calls that nobody can read safely. The goal is not maximum flexibility; it is a call whose meaning is obvious.", "Quando um app solicita relatório, envia notificação ou pesquisa catálogo, ele chama funções com opções. Algumas entradas são essenciais, como id do cliente. Outras têm padrões sensatos, como tamanho da página ou idioma. Parâmetros transformam rotina fixa em serviço reutilizável, mas parâmetros demais e pouco claros criam chamadas que ninguém lê com segurança. O objetivo não é flexibilidade máxima; é uma chamada cujo significado seja óbvio."),
      text("Python supports positional arguments, keyword arguments, default values and keyword-only parameters. Each exists to make a different contract clearer. Positional arguments are concise when order is natural. Keywords are safer when several values have the same type or the call contains optional behavior. Defaults reduce repetition for the common case. Keyword-only parameters prevent a caller from guessing what a sequence of True, False and numbers means.", "Python oferece argumentos posicionais, nomeados, valores padrão e parâmetros apenas nomeados. Cada recurso torna um tipo de contrato mais claro. Posicionais são concisos quando a ordem é natural. Nomeados são mais seguros quando vários valores têm o mesmo tipo ou a chamada possui comportamento opcional. Padrões reduzem repetição no caso comum. Parâmetros apenas nomeados impedem adivinhar o significado de uma sequência de True, False e números."),
      heading("🧩 Physical analogy — ordering a customizable meal", "🧩 Analogia física — pedir uma refeição personalizável"),
      text("At a restaurant, the dish name is required, while sauce and side may have defaults. Saying “pasta, large, no cheese” works only if everyone agrees on the order. Saying dish=\"pasta\", size=\"large\", cheese=False is longer but unambiguous. A keyword-only option is like a checkbox on the order form: the customer must identify the option by name rather than rely on position.", "Num restaurante, o prato é obrigatório, enquanto molho e acompanhamento podem ter padrões. Dizer “massa, grande, sem queijo” funciona apenas se todos concordam com a ordem. Dizer dish=\"pasta\", size=\"large\", cheese=False é mais longo, porém inequívoco. Uma opção apenas nomeada é como uma caixa no formulário: o cliente precisa identificar a opção pelo nome, não pela posição."),
      text("Defaults are not backup values for invalid data. They apply only when the caller omits an argument. If the caller explicitly passes None, an empty string or a negative number, the function receives that value and must decide whether it is valid. This distinction prevents bugs where a default silently hides a real data problem.", "Padrões não são valores de reserva para dados inválidos. Eles se aplicam apenas quando quem chama omite o argumento. Se a chamada passa explicitamente None, string vazia ou número negativo, a função recebe esse valor e precisa decidir se é válido. Essa distinção evita bugs em que um padrão esconde silenciosamente um problema real de dados."),
      heading("🐍 Fundamentals 1 — positional and keyword arguments", "🐍 Fundamentos 1 — argumentos posicionais e nomeados"),
      code("def delivery_label(city, days):\n    return f\"{city}: {days} day(s)\"\n\nprint(delivery_label(\"Toronto\", 2))\nprint(delivery_label(days=3, city=\"Oshawa\"))", "def rotulo_entrega(cidade, dias):\n    return f\"{cidade}: {dias} dia(s)\"\n\nprint(rotulo_entrega(\"Toronto\", 2))\nprint(rotulo_entrega(dias=3, cidade=\"Oshawa\"))"),
      text("The first call binds by position. The second binds by name, so keyword order may change. Once a call uses a keyword argument, ordinary positional arguments cannot appear after it. Python raises SyntaxError for that invalid ordering before the program runs. Prefer keywords when a reader might confuse two numbers, booleans or strings.", "A primeira chamada liga por posição. A segunda liga por nome, então a ordem dos nomeados pode mudar. Depois que uma chamada usa argumento nomeado, argumentos posicionais comuns não podem aparecer após ele. Python gera SyntaxError para essa ordem inválida antes de rodar. Prefira nomes quando um leitor pode confundir dois números, booleanos ou textos."),
      heading("🐍 Fundamentals 2 — safe default values", "🐍 Fundamentos 2 — valores padrão seguros"),
      code("def shipping_cost(weight, rate=2.5):\n    if weight < 0 or rate < 0:\n        raise ValueError(\"negative shipping value\")\n    return round(weight * rate, 2)\n\nprint(shipping_cost(4))\nprint(shipping_cost(4, rate=3.1))", "def custo_frete(peso, taxa=2.5):\n    if peso < 0 or taxa < 0:\n        raise ValueError(\"negative shipping value\")\n    return round(peso * taxa, 2)\n\nprint(custo_frete(4))\nprint(custo_frete(4, taxa=3.1))"),
      text("Parameters without defaults must appear before parameters with defaults. Otherwise Python raises SyntaxError: non-default argument follows default argument. The required-first rule makes calls predictable. A default should represent the most common safe behavior and should be visible in documentation and tests. Changing a public default later can change many callers even when their code remains unchanged.", "Parâmetros sem padrão precisam aparecer antes dos que possuem padrão. Caso contrário Python gera SyntaxError: non-default argument follows default argument. A regra dos obrigatórios primeiro torna chamadas previsíveis. Um padrão deve representar o comportamento seguro mais comum e aparecer na documentação e nos testes. Alterar um padrão público depois pode mudar muitos chamadores mesmo sem alteração no código deles."),
      heading("🐍 Fundamentals 3 — avoid mutable defaults", "🐍 Fundamentos 3 — evite padrões mutáveis"),
      code("def add_tag(tag, tags=None):\n    if tags is None:\n        tags = []\n    tags.append(tag)\n    return tags\n\nprint(add_tag(\"python\"))\nprint(add_tag(\"data\"))  # a fresh list, not [\"python\", \"data\"]", "def adicionar_tag(tag, tags=None):\n    if tags is None:\n        tags = []\n    tags.append(tag)\n    return tags\n\nprint(adicionar_tag(\"python\"))\nprint(adicionar_tag(\"dados\"))  # lista nova, não [\"python\", \"dados\"]"),
      text("Default expressions are evaluated once when def runs, not once per call. A list default would therefore be shared by every call and remember previous changes. Use None as a sentinel, then create a new list inside the function. Immutable defaults such as numbers, strings, tuples and None are normally safe because the function cannot modify the original object in place.", "Expressões padrão são avaliadas uma vez quando def roda, não uma vez por chamada. Uma lista padrão seria compartilhada por todas as chamadas e lembraria mudanças anteriores. Use None como sentinela e crie uma nova lista dentro da função. Padrões imutáveis como números, strings, tuplas e None normalmente são seguros porque a função não altera o objeto original no lugar."),
      heading("🐍 Fundamentals 4 — keyword-only options", "🐍 Fundamentos 4 — opções apenas nomeadas"),
      code("def export_report(records, *, format=\"csv\", include_header=True):\n    if format not in {\"csv\", \"json\"}:\n        raise ValueError(\"unsupported format\")\n    return f\"{format}:{len(records)}:{include_header}\"\n\nprint(export_report([1, 2], format=\"json\", include_header=False))", "def exportar_relatorio(registros, *, formato=\"csv\", incluir_cabecalho=True):\n    if formato not in {\"csv\", \"json\"}:\n        raise ValueError(\"unsupported format\")\n    return f\"{formato}:{len(registros)}:{incluir_cabecalho}\"\n\nprint(exportar_relatorio([1, 2], formato=\"json\", incluir_cabecalho=False))"),
      text("The bare * means every parameter after it must be supplied by name. This is valuable for options whose order is arbitrary or whose values could be mistaken. export_report(records, \"json\", False) forces the reader to remember positions. The keyword-only version makes the call self-explanatory and gives future versions room to add options without breaking positional order.", "O * isolado significa que todo parâmetro depois dele precisa ser fornecido pelo nome. Isso é valioso para opções cuja ordem é arbitrária ou cujos valores podem ser confundidos. export_report(records, \"json\", False) obriga o leitor a lembrar posições. A versão apenas nomeada torna a chamada autoexplicativa e permite adicionar opções sem quebrar ordem posicional."),
      heading("🏗️ Real scenario 1 — e-commerce price quote", "🏗️ Cenário real 1 — cotação de preço no e-commerce"),
      text("A storefront calculates a quote with an optional discount and a keyword-only tax rate. Quantity and unit price are natural positional inputs; discount and tax are policy options that deserve names. The function validates each percentage so a malformed promotion cannot produce a negative or exaggerated invoice.", "Uma loja calcula cotação com desconto opcional e taxa de imposto apenas nomeada. Quantidade e preço unitário são entradas posicionais naturais; desconto e imposto são opções de política que merecem nomes. A função valida cada percentual para que promoção malformada não produza fatura negativa ou exagerada."),
      code("def quote(quantity, unit_price, discount=0, *, tax=0.13):\n    if quantity < 0 or unit_price < 0:\n        raise ValueError(\"negative quote value\")\n    if not 0 <= discount <= 1 or not 0 <= tax <= 1:\n        raise ValueError(\"rate out of range\")\n    subtotal = quantity * unit_price * (1 - discount)\n    return round(subtotal * (1 + tax), 2)\n\nprint(quote(3, 20))\nprint(quote(3, 20, discount=0.1, tax=0.05))", "def cotacao(quantidade, preco_unitario, desconto=0, *, imposto=0.13):\n    if quantidade < 0 or preco_unitario < 0:\n        raise ValueError(\"negative quote value\")\n    if not 0 <= desconto <= 1 or not 0 <= imposto <= 1:\n        raise ValueError(\"rate out of range\")\n    subtotal = quantidade * preco_unitario * (1 - desconto)\n    return round(subtotal * (1 + imposto), 2)\n\nprint(cotacao(3, 20))\nprint(cotacao(3, 20, desconto=0.1, imposto=0.05))"),
      text("The signature communicates policy. A caller may omit the common discount and tax, but an unusual rate must be named. Tests should cover omitted defaults, explicit keyword overrides and invalid ranges. Testing only the fully specified call misses the exact behavior defaults were designed to provide.", "A assinatura comunica política. Quem chama pode omitir desconto e imposto comuns, mas uma taxa incomum precisa ser nomeada. Testes devem cobrir padrões omitidos, substituições nomeadas e intervalos inválidos. Testar apenas a chamada totalmente especificada perde justamente o comportamento que os padrões foram criados para fornecer."),
      heading("🏗️ Real scenario 2 — education feedback generator", "🏗️ Cenário real 2 — gerador de feedback educacional"),
      text("A learning platform produces a message from the learner name and score, with optional language and a keyword-only flag that controls whether the numeric score appears. Keyword arguments make the call readable at the point where privacy and presentation choices are made.", "Uma plataforma de aprendizagem produz mensagem com nome e nota, com idioma opcional e uma flag apenas nomeada que controla se a nota numérica aparece. Argumentos nomeados tornam a chamada legível no ponto em que escolhas de privacidade e apresentação são feitas."),
      code("def feedback(name, score, language=\"en\", *, show_score=True):\n    if not 0 <= score <= 100:\n        raise ValueError(\"score out of range\")\n    passed = score >= 70\n    status = {\"en\": (\"passed\", \"review\"), \"pt\": (\"aprovado\", \"revisar\")}[language][0 if passed else 1]\n    suffix = f\" ({score})\" if show_score else \"\"\n    return f\"{name.strip()}: {status}{suffix}\"\n\nprint(feedback(\" Ana \", 82, language=\"pt\", show_score=False))", "def feedback(nome, nota, idioma=\"en\", *, mostrar_nota=True):\n    if not 0 <= nota <= 100:\n        raise ValueError(\"score out of range\")\n    aprovado = nota >= 70\n    status = {\"en\": (\"passed\", \"review\"), \"pt\": (\"aprovado\", \"revisar\")}[idioma][0 if aprovado else 1]\n    sufixo = f\" ({nota})\" if mostrar_nota else \"\"\n    return f\"{nome.strip()}: {status}{sufixo}\"\n\nprint(feedback(\" Ana \", 82, idioma=\"pt\", mostrar_nota=False))"),
      heading("⚠️ Common errors — parameter order and shared state", "⚠️ Erros comuns — ordem de parâmetros e estado compartilhado"),
      warning("SyntaxError: positional argument follows keyword argument means a positional value appeared after a named one. TypeError: got multiple values for argument means the same parameter was filled by position and again by keyword. TypeError: unexpected keyword argument often indicates a misspelled parameter name. SyntaxError: non-default argument follows default argument means a required parameter was declared after an optional one. Shared data appearing across calls usually points to a mutable default such as [].", "SyntaxError: positional argument follows keyword argument significa valor posicional após um nomeado. TypeError: got multiple values for argument indica que o mesmo parâmetro foi preenchido por posição e por nome. TypeError: unexpected keyword argument normalmente é nome digitado errado. SyntaxError: non-default argument follows default argument indica parâmetro obrigatório após opcional. Dados compartilhados entre chamadas normalmente apontam padrão mutável como []."),
      text("Another design error is using None as a default but also accepting None as meaningful business data. In that case the function cannot distinguish “omitted” from “explicitly empty”. Advanced code may use a unique sentinel object. For this level, choose contracts where None clearly means omitted, document that choice and reject ambiguous values early.", "Outro erro de design é usar None como padrão e também aceitar None como dado de negócio significativo. Nesse caso a função não distingue “omitido” de “explicitamente vazio”. Código avançado pode usar objeto sentinela único. Neste nível, escolha contratos em que None signifique claramente omitido, documente a decisão e rejeite valores ambíguos cedo."),
      heading("💡 Pro tip — optimize signatures for the common call", "💡 Dica pro — otimize assinaturas para a chamada comum"),
      tip("Place the few essential inputs first, give conservative defaults to genuinely optional behavior and make risky policy choices keyword-only. Then write examples for the default call and one override. If a function needs many booleans, consider a dictionary or configuration object later because report(True, False, True, False) is not a readable API.", "Coloque poucas entradas essenciais primeiro, dê padrões conservadores ao comportamento realmente opcional e torne escolhas arriscadas apenas nomeadas. Depois escreva exemplos para a chamada padrão e uma substituição. Se a função precisa de muitos booleanos, considere dicionário ou objeto de configuração depois, porque report(True, False, True, False) não é API legível."),
      heading("📋 Recap and bridge to documentation", "📋 Resumo e ponte para documentação"),
      text("You can now choose positional or keyword calls, create safe defaults, avoid shared mutable defaults and require keywords for options. A good signature exposes a contract, but callers still need to know valid ranges, errors and returned values. The next phase turns that contract into a docstring that tools, teammates and your future self can read.", "Agora você consegue escolher chamadas posicionais ou nomeadas, criar padrões seguros, evitar padrões mutáveis compartilhados e exigir nomes para opções. Uma boa assinatura expõe contrato, mas quem chama ainda precisa conhecer intervalos válidos, erros e retornos. A próxima fase transforma esse contrato em docstring que ferramentas, colegas e você no futuro podem ler."),
    ],
  },
  exercises: [
    exercise(
      "p14-guided-greeting",
      b("🟢 Guided — add a safe default", "🟢 Guiado — adicione um padrão seguro"),
      b("Complete greet(name, language=\"en\") so omitted language returns an English greeting and language=\"pt\" returns Portuguese.", "Complete greet(name, language=\"en\") para que idioma omitido retorne saudação em inglês e language=\"pt\" retorne português."),
      "def greet(name, language=___):\n    clean = name.strip()\n    if language == \"pt\":\n        return f\"Olá, {clean}\"\n    return f\"Hello, {clean}\"",
      b("Hello, Ana", "Hello, Ana"),
      [b("The default string is \"en\".", "A string padrão é \"en\"."), b("Keep the default in the signature.", "Mantenha o padrão na assinatura."), b("Return the message; do not print inside.", "Retorne a mensagem; não imprima dentro.")],
      [{ kind: "function", value: "greet" }, { kind: "node", value: "arguments" }, { kind: "node", value: "If" }, { kind: "node", value: "Return", minCount: 2 }],
      exactTest("p14-greet-visible", b("Uses the default language", "Usa o idioma padrão"), "print(greet(\"Ana\"))", b("Hello, Ana", "Hello, Ana"), 60, [{ kind: "function", value: "greet" }, { kind: "node", value: "Return" }], false),
      exactTest("p14-greet-accented", b("Uses Portuguese with accented name", "Usa português com nome acentuado"), "print(greet(\" Érica \", language=\"pt\"))", b("Olá, Érica", "Olá, Érica"), 40, [{ kind: "function", value: "greet" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], true),
      "guided",
      b("Use a default for the common call and a keyword override.", "Usar padrão para chamada comum e substituição nomeada."),
      b("Localized messages in support, learning and commerce apps.", "Mensagens localizadas em apps de suporte, aprendizagem e comércio."),
      { en: ["English is default", "Portuguese works by keyword", "Name is stripped"], pt: ["Inglês é padrão", "Português funciona por nome", "Nome é limpo"] },
      { en: ["Default after the function body", "Printing instead of returning", "Ignoring language"], pt: ["Padrão no corpo", "Imprimir em vez de retornar", "Ignorar idioma"] },
    ),
    exercise(
      "p14-complete-fee",
      b("🟡 Complete — positional and keyword price options", "🟡 Complete — opções posicionais e nomeadas de preço"),
      b("Finish service_fee(amount, rate=0.05, *, minimum=0). Validate non-negative inputs and return the greater of amount * rate and minimum.", "Termine service_fee(amount, rate=0.05, *, minimum=0). Valide entradas não negativas e retorne o maior entre amount * rate e minimum."),
      "def service_fee(amount, rate=0.05, *, minimum=0):\n    if amount < 0 or rate < 0 or minimum < 0:\n        raise ValueError(\"negative fee value\")\n    calculated = ___\n    return round(___, 2)",
      b("5.0", "5.0"),
      [b("calculated = amount * rate", "calculated = amount * rate"), b("Use max(calculated, minimum).", "Use max(calculated, minimum)."), b("minimum must be passed by keyword.", "minimum precisa ser passado por nome.")],
      [{ kind: "function", value: "service_fee" }, { kind: "call", value: "max" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p14-fee-visible", b("Uses the default rate", "Usa taxa padrão"), "print(service_fee(100))", b("5.0", "5.0"), 60, [{ kind: "function", value: "service_fee" }, { kind: "call", value: "max" }, { kind: "node", value: "Return" }], false),
      exactTest("p14-fee-large-minimum", b("Uses an explicit minimum", "Usa mínimo explícito"), "print(service_fee(1000000, rate=0.001, minimum=1500))", b("1500", "1500"), 40, [{ kind: "function", value: "service_fee" }, { kind: "call", value: "max" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Combine defaults with a keyword-only business rule.", "Combinar padrões com regra de negócio apenas nomeada."),
      b("Service charges, platform fees and minimum billing rules.", "Taxas de serviço, plataforma e faturamento mínimo."),
      { en: ["Default rate works", "Minimum is enforced", "Invalid values raise"], pt: ["Taxa padrão funciona", "Mínimo é respeitado", "Inválidos geram erro"] },
      { en: ["Using min instead of max", "Forgetting keyword-only star", "Allowing negative rates"], pt: ["Usar min em vez de max", "Esquecer * apenas nomeado", "Permitir taxas negativas"] },
    ),
    exercise(
      "p14-zero-tags",
      b("🔴 From scratch — avoid a mutable default", "🔴 Do zero — evite padrão mutável"),
      b("Write collect_tag(tag, tags=None). Create a fresh list when tags is None, append a stripped lowercase tag and return the list.", "Escreva collect_tag(tag, tags=None). Crie lista nova quando tags for None, adicione tag limpa em minúsculas e retorne a lista."),
      "def collect_tag(tag, tags=None):\n    pass",
      b("['python']", "['python']"),
      [b("Use if tags is None.", "Use if tags is None."), b("Create tags = [] inside that branch.", "Crie tags = [] dentro desse ramo."), b("Append tag.strip().lower().", "Adicione tag.strip().lower().")],
      [{ kind: "function", value: "collect_tag" }, { kind: "node", value: "If" }, { kind: "call", value: "append" }, { kind: "node", value: "Return" }],
      exactTest("p14-tags-visible", b("Builds a provided list", "Monta uma lista fornecida"), "print(collect_tag(\" Python \", []))", b("['python']", "['python']"), 60, [{ kind: "function", value: "collect_tag" }, { kind: "call", value: "append" }, { kind: "node", value: "Return" }], false),
      exactTest("p14-tags-fresh", b("Creates independent default lists", "Cria listas padrão independentes"), "print(collect_tag(\"café\"))\nprint(collect_tag(\"dados\"))", b("['café']\n['dados']", "['café']\n['dados']"), 40, [{ kind: "function", value: "collect_tag" }, { kind: "node", value: "If" }, { kind: "call", value: "append" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Use None as a safe sentinel for a new mutable object.", "Usar None como sentinela segura para objeto mutável novo."),
      b("Collecting tags, filters and temporary selections.", "Coleta de tags, filtros e seleções temporárias."),
      { en: ["Fresh list per omitted call", "Normalizes text", "Supports a provided list"], pt: ["Lista nova por chamada omitida", "Normaliza texto", "Aceita lista fornecida"] },
      { en: ["Using tags=[] in signature", "Forgetting to append", "Returning before append"], pt: ["Usar tags=[] na assinatura", "Esquecer append", "Retornar antes de adicionar"] },
    ),
    exercise(
      "p14-transfer",
      b("🟣 Challenge — design a readable export API", "🟣 Desafio — projete uma API de exportação legível"),
      b("Write export_name(base, extension=\"csv\", *, compressed=False). Strip base, reject an empty base or unsupported extension, and return base.extension or base.extension.gz.", "Escreva export_name(base, extension=\"csv\", *, compressed=False). Limpe base, rejeite base vazio ou extensão não suportada e retorne base.extension ou base.extension.gz."),
      "def export_name(base, extension=\"csv\", *, compressed=False):\n    pass",
      b("report.csv.gz", "report.csv.gz"),
      [b("Allowed extensions are csv and json.", "Extensões permitidas são csv e json."), b("Build suffix = \".gz\" if compressed else \"\".", "Monte suffix = \".gz\" if compressed else \"\"."), b("compressed must stay keyword-only.", "compressed deve continuar apenas nomeado.")],
      [{ kind: "function", value: "export_name" }, { kind: "node", value: "If", minCount: 2 }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p14-export-visible", b("Creates a compressed CSV name", "Cria nome CSV compactado"), "print(export_name(\" report \", compressed=True))", b("report.csv.gz", "report.csv.gz"), 60, [{ kind: "function", value: "export_name" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], false),
      exactTest("p14-export-empty", b("Rejects an empty base name", "Rejeita nome base vazio"), "try:\n    export_name(\"   \", extension=\"json\")\nexcept ValueError as error:\n    print(error)", b("empty base", "empty base"), 40, [{ kind: "function", value: "export_name" }, { kind: "node", value: "Raise" }], true),
      "challenge",
      b("Design required, default and keyword-only parameters intentionally.", "Projetar parâmetros obrigatórios, padrão e apenas nomeados intencionalmente."),
      b("Export tools, report generators and file pipelines.", "Ferramentas de exportação, geradores de relatório e pipelines de arquivos."),
      { en: ["Readable signature", "Validates extension", "Keyword-only compression"], pt: ["Assinatura legível", "Valida extensão", "Compactação apenas nomeada"] },
      { en: ["Accepting any extension", "Leaving spaces in base", "Making all options positional"], pt: ["Aceitar qualquer extensão", "Deixar espaços no base", "Tornar tudo posicional"] },
    ),
  ],
  quiz: [
    { id: "q14-v11-1", question: b("When is a default expression evaluated?", "Quando uma expressão padrão é avaliada?"), options: [b("Every time the function returns", "Toda vez que a função retorna"), b("Once when the def statement runs", "Uma vez quando def executa"), b("Only in hidden tests", "Apenas em testes ocultos"), b("After every print", "Depois de cada print")], correctIndex: 1, explanation: b("This is why mutable defaults can retain changes across calls.", "Por isso padrões mutáveis podem manter mudanças entre chamadas.") },
    { id: "q14-v11-2", question: b("What does a bare * in a signature do?", "O que um * isolado na assinatura faz?"), options: [b("Multiplies all parameters", "Multiplica parâmetros"), b("Makes previous parameters optional", "Torna anteriores opcionais"), b("Makes following parameters keyword-only", "Torna os seguintes apenas nomeados"), b("Imports all names", "Importa todos os nomes")], correctIndex: 2, explanation: b("Callers must name every parameter that appears after the bare star.", "Quem chama precisa nomear todo parâmetro que aparece após o asterisco isolado.") },
    { id: "q14-v11-3", question: b("Which is the safe default for a list you plan to mutate?", "Qual é o padrão seguro para uma lista que será alterada?"), options: [b("None, then create a list inside", "None, depois criar lista dentro"), b("[] directly in the signature", "[] diretamente na assinatura"), b("A global list", "Uma lista global"), b("The string \"[]\"", "A string \"[]\"")], correctIndex: 0, explanation: b("None avoids one list being shared by every call.", "None evita que uma lista seja compartilhada por todas as chamadas.") },
    { id: "q14-v11-4", question: b("Why use keyword arguments for multiple booleans?", "Por que usar argumentos nomeados para vários booleanos?"), options: [b("They run faster", "Eles rodam mais rápido"), b("They disable validation", "Eles desativam validação"), b("They automatically become strings", "Eles viram strings automaticamente"), b("They make each choice visible at the call site", "Eles deixam cada escolha visível na chamada")], correctIndex: 3, explanation: b("show_total=True is much clearer than an unexplained True in a sequence.", "show_total=True é muito mais claro que um True sem explicação numa sequência.") },
  ],
  exam: {
    title: b("Configurable delivery quote", "Cotação de entrega configurável"),
    scenario: b("Create delivery_quote(distance, rate=1.8, *, priority=False, minimum=5). Validate values, calculate distance * rate, add 25% for priority and enforce the minimum.", "Crie delivery_quote(distance, rate=1.8, *, priority=False, minimum=5). Valide valores, calcule distance * rate, adicione 25% para prioridade e aplique o mínimo."),
    requirements: { en: ["Use the exact signature with two keyword-only options", "Reject any negative numeric input with ValueError(\"negative delivery value\")", "Use default rate 1.8 and minimum 5", "Priority multiplies the calculated cost by 1.25", "Return the rounded maximum of calculated cost and minimum"], pt: ["Use a assinatura exata com duas opções apenas nomeadas", "Rejeite qualquer número negativo com ValueError(\"negative delivery value\")", "Use taxa padrão 1.8 e mínimo 5", "Prioridade multiplica o custo por 1.25", "Retorne o maior custo arredondado entre cálculo e mínimo"] },
    starterCode: "def delivery_quote(distance, rate=1.8, *, priority=False, minimum=5):\n    pass",
    expectedOutput: b("18.0\n22.5", "18.0\n22.5"),
    testCases: [
      exactTest("p14-exam-visible", b("Uses defaults and priority override", "Usa padrões e prioridade"), "print(delivery_quote(10))\nprint(delivery_quote(10, priority=True))", b("18.0\n22.5", "18.0\n22.5"), 60, [{ kind: "function", value: "delivery_quote" }, { kind: "call", value: "max" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], false),
      exactTest("p14-exam-minimum", b("Enforces the minimum on a tiny distance", "Aplica mínimo em distância pequena"), "print(delivery_quote(0, minimum=7))", b("7", "7"), 20, [{ kind: "function", value: "delivery_quote" }, { kind: "call", value: "max" }, { kind: "node", value: "Return" }], true),
      exactTest("p14-exam-hidden-hardening", b("Rejects a negative distance", "Rejeita distância negativa"), "try:\n    delivery_quote(-1)\nexcept ValueError as error:\n    print(error)", b("negative delivery value", "negative delivery value"), 20, [{ kind: "function", value: "delivery_quote" }, { kind: "node", value: "Raise" }], true),
    ],
  },
}

export const phase15: Phase = {
  id: 15,
  title: b("Docstrings and Function Contracts", "Docstrings e Contratos de Função"),
  description: b("Document behavior, inputs, returns and errors so functions can be used without reading their implementation.", "Documente comportamento, entradas, retornos e erros para usar funções sem ler a implementação."),
  icon: "📝",
  libraries: [],
  track: 'core',
  stage: 'base',
  estimatedHours: 7,
  lesson: {
    title: b("Docstrings: documentation that lives with the code", "Docstrings: documentação que vive com o código"),
    blocks: [
      heading("🌍 World hook — professional code is read more often than it is written", "🌍 Gancho do mundo real — código profissional é lido mais vezes do que escrito"),
      text("A teammate sees a function named calculate_risk but does not know whether the score is 0–1 or 0–100, whether negative values are accepted or what exception can occur. Reading the whole implementation every time is expensive and unreliable. A docstring places the public contract beside the function so editors, help(), generated documentation and human reviewers can answer those questions immediately.", "Um colega vê uma função chamada calculate_risk, mas não sabe se a pontuação vai de 0–1 ou 0–100, se negativos são aceitos ou qual exceção pode ocorrer. Ler toda a implementação sempre é caro e pouco confiável. Uma docstring coloca o contrato público junto da função para que editores, help(), documentação gerada e revisores respondam essas perguntas imediatamente."),
      text("Documentation is not decoration. It is part of the interface. A correct docstring describes what callers may rely on: purpose, parameter meaning, return value, important side effects and expected errors. It should not narrate every implementation line. Internal algorithms may change while the public contract stays stable, so documentation must describe behavior rather than temporary mechanics.", "Documentação não é decoração. Ela faz parte da interface. Uma docstring correta descreve aquilo em que quem chama pode confiar: objetivo, significado dos parâmetros, retorno, efeitos colaterais importantes e erros esperados. Ela não deve narrar cada linha da implementação. Algoritmos internos podem mudar enquanto o contrato público permanece, então a documentação deve descrever comportamento e não mecânica temporária."),
      heading("🧩 Physical analogy — the instruction label on a tool", "🧩 Analogia física — a etiqueta de instrução numa ferramenta"),
      text("A power tool may work perfectly, but a user still needs the label that states voltage, intended use, safety limits and output. The label does not explain every gear inside the motor. A function docstring plays the same role: what the tool does, what goes in, what comes out and what conditions make it refuse the operation.", "Uma ferramenta elétrica pode funcionar perfeitamente, mas o usuário ainda precisa da etiqueta com tensão, uso previsto, limites de segurança e resultado. A etiqueta não explica cada engrenagem dentro do motor. A docstring exerce o mesmo papel: o que a ferramenta faz, o que entra, o que sai e em quais condições ela recusa a operação."),
      text("A misleading label is worse than no label because it creates false confidence. The same is true for stale docstrings. When behavior changes, update tests and documentation in the same change. A review should reject a function whose code returns a float while the docstring promises an integer, even if current examples happen to pass.", "Uma etiqueta enganosa é pior que nenhuma porque cria confiança falsa. O mesmo vale para docstrings desatualizadas. Quando o comportamento muda, atualize testes e documentação na mesma mudança. Uma revisão deve rejeitar função cujo código retorna float enquanto a docstring promete inteiro, mesmo que exemplos atuais passem."),
      heading("🐍 Fundamentals 1 — place a docstring correctly", "🐍 Fundamentos 1 — coloque uma docstring corretamente"),
      code("def area(width, height):\n    \"\"\"Return the rectangular area for non-negative dimensions.\"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"negative dimension\")\n    return width * height\n\nprint(area.__doc__)\nhelp(area)", "def area(largura, altura):\n    \"\"\"Retorne a área retangular para dimensões não negativas.\"\"\"\n    if largura < 0 or altura < 0:\n        raise ValueError(\"negative dimension\")\n    return largura * altura\n\nprint(area.__doc__)\nhelp(area)"),
      text("The docstring is the first statement inside the function and uses a string literal, commonly triple quotes. A comment beginning with # is not stored in function.__doc__ and help() cannot use it. The first line should be a short command-style summary such as “Return”, “Calculate”, “Normalize” or “Validate”.", "A docstring é a primeira instrução dentro da função e usa literal de string, normalmente aspas triplas. Um comentário com # não fica armazenado em function.__doc__ e help() não consegue usá-lo. A primeira linha deve ser resumo curto em estilo de comando, como “Retorne”, “Calcule”, “Normalize” ou “Valide”."),
      heading("🐍 Fundamentals 2 — document parameters and return values", "🐍 Fundamentos 2 — documente parâmetros e retornos"),
      code("def format_currency(amount, currency=\"CAD\"):\n    \"\"\"Format a non-negative amount with a currency code.\n\n    Args:\n        amount: Numeric value greater than or equal to zero.\n        currency: Three-letter code shown before the amount.\n\n    Returns:\n        A string such as \"CAD 12.50\".\n    \"\"\"\n    if amount < 0:\n        raise ValueError(\"negative amount\")\n    return f\"{currency} {amount:.2f}\"", "def formatar_moeda(valor, moeda=\"CAD\"):\n    \"\"\"Formate um valor não negativo com um código de moeda.\n\n    Args:\n        valor: Número maior ou igual a zero.\n        moeda: Código de três letras exibido antes do valor.\n\n    Returns:\n        Uma string como \"CAD 12.50\".\n    \"\"\"\n    if valor < 0:\n        raise ValueError(\"negative amount\")\n    return f\"{moeda} {valor:.2f}\""),
      text("Parameter documentation should explain meaning and constraints, not repeat the name. “amount: the amount” adds no information. “Numeric value greater than or equal to zero” tells a caller what is valid. The return section should state type and semantic meaning. If the function mutates an input or writes a file, document that side effect clearly because callers cannot infer it from the returned value.", "A documentação do parâmetro deve explicar significado e restrições, não repetir o nome. “valor: o valor” não acrescenta informação. “Número maior ou igual a zero” informa o que é válido. A seção de retorno deve indicar tipo e significado. Se a função altera entrada ou grava arquivo, documente esse efeito claramente porque quem chama não consegue inferi-lo pelo retorno."),
      heading("🐍 Fundamentals 3 — document raised errors", "🐍 Fundamentos 3 — documente erros gerados"),
      code("def average(values):\n    \"\"\"Return the arithmetic mean of a non-empty numeric sequence.\n\n    Raises:\n        ValueError: If values is empty.\n    \"\"\"\n    if not values:\n        raise ValueError(\"empty values\")\n    return sum(values) / len(values)", "def media(valores):\n    \"\"\"Retorne a média aritmética de uma sequência numérica não vazia.\n\n    Raises:\n        ValueError: Se valores estiver vazio.\n    \"\"\"\n    if not valores:\n        raise ValueError(\"empty values\")\n    return sum(valores) / len(valores)"),
      text("Document errors that are part of normal contract enforcement. You do not need to list every hypothetical Python failure. If a function deliberately raises ValueError for empty data, callers may catch that condition or validate earlier. The docstring and actual error message should agree. Tests should verify both behavior and the documentation signal when the error is an important public rule.", "Documente erros que fazem parte da validação normal do contrato. Não é necessário listar toda falha hipotética do Python. Se a função gera ValueError para dados vazios, quem chama pode tratar ou validar antes. A docstring e a mensagem real devem concordar. Testes devem verificar comportamento e sinal documental quando o erro é regra pública importante."),
      heading("🐍 Fundamentals 4 — examples that demonstrate the contract", "🐍 Fundamentos 4 — exemplos que demonstram o contrato"),
      code("def slugify(title):\n    \"\"\"Return a lowercase hyphenated slug.\n\n    Example:\n        >>> slugify(\"Python Basics\")\n        'python-basics'\n    \"\"\"\n    clean = title.strip().lower()\n    return \"-\".join(clean.split())", "def criar_slug(titulo):\n    \"\"\"Retorne um slug minúsculo separado por hífens.\n\n    Example:\n        >>> criar_slug(\"Python Básico\")\n        'python-básico'\n    \"\"\"\n    limpo = titulo.strip().lower()\n    return \"-\".join(limpo.split())"),
      text("A short example is valuable when formatting or edge behavior is easier to show than describe. Examples must be executable and consistent with tests. Avoid examples that reveal only the happy path when empty or accented text changes behavior. The example is not a replacement for parameter, return and error documentation; it is a concrete demonstration of the same contract.", "Um exemplo curto é valioso quando formatação ou comportamento de limite é mais fácil de mostrar que descrever. Exemplos precisam ser executáveis e coerentes com testes. Evite exemplos que mostram apenas caminho feliz quando texto vazio ou acentuado muda o comportamento. O exemplo não substitui documentação de parâmetros, retorno e erros; é uma demonstração concreta do mesmo contrato."),
      heading("🏗️ Real scenario 1 — logistics route estimator", "🏗️ Cenário real 1 — estimador de rota logística"),
      text("A route estimator receives distance and average speed. Operations staff may reuse it in a dashboard, an export and an alert. The docstring states units because 80 could mean kilometers or miles, and 50 could mean kilometers per hour or minutes. Unit ambiguity is a real production defect that documentation and naming should eliminate.", "Um estimador de rota recebe distância e velocidade média. A operação pode reutilizá-lo num painel, exportação e alerta. A docstring indica unidades porque 80 pode significar quilômetros ou milhas, e 50 pode significar quilômetros por hora ou minutos. Ambiguidade de unidade é defeito real que documentação e nomes devem eliminar."),
      code("def travel_hours(distance_km, speed_kmh):\n    \"\"\"Return estimated travel time in hours.\n\n    Args:\n        distance_km: Non-negative route distance in kilometres.\n        speed_kmh: Average speed greater than zero in km/h.\n\n    Raises:\n        ValueError: If distance is negative or speed is not positive.\n    \"\"\"\n    if distance_km < 0 or speed_kmh <= 0:\n        raise ValueError(\"invalid route value\")\n    return round(distance_km / speed_kmh, 2)", "def horas_viagem(distancia_km, velocidade_kmh):\n    \"\"\"Retorne o tempo estimado de viagem em horas.\n\n    Args:\n        distancia_km: Distância não negativa em quilômetros.\n        velocidade_kmh: Velocidade média positiva em km/h.\n\n    Raises:\n        ValueError: Se a distância for negativa ou velocidade não positiva.\n    \"\"\"\n    if distancia_km < 0 or velocidade_kmh <= 0:\n        raise ValueError(\"invalid route value\")\n    return round(distancia_km / velocidade_kmh, 2)"),
      text("The function name and parameter suffixes already help, while the docstring completes the contract. Good documentation is layered: meaningful names first, concise summary second, details only where they reduce uncertainty. A huge docstring cannot rescue a confusing function, and a clear function still needs documentation for units, ranges and errors that names cannot fully encode.", "O nome da função e os sufixos dos parâmetros já ajudam, enquanto a docstring completa o contrato. Boa documentação é em camadas: nomes significativos primeiro, resumo conciso depois, detalhes apenas onde reduzem incerteza. Docstring enorme não salva função confusa, e função clara ainda precisa documentar unidades, intervalos e erros que nomes não codificam totalmente."),
      heading("🏗️ Real scenario 2 — media content duration", "🏗️ Cenário real 2 — duração de conteúdo de mídia"),
      text("A media tool formats seconds as minutes and seconds. Editors need to know whether negative values are rejected and whether the result includes leading zeros. The example makes the exact display contract visible, while the test protects it from accidental changes during refactoring.", "Uma ferramenta de mídia formata segundos em minutos e segundos. Editores precisam saber se negativos são rejeitados e se o resultado inclui zero à esquerda. O exemplo torna o contrato exato de exibição visível, enquanto o teste o protege contra mudanças acidentais durante refatoração."),
      code("def format_duration(total_seconds):\n    \"\"\"Return a duration using MM:SS format.\n\n    Args:\n        total_seconds: Non-negative whole number of seconds.\n\n    Returns:\n        A string such as \"03:07\".\n\n    Raises:\n        ValueError: If total_seconds is negative.\n    \"\"\"\n    if total_seconds < 0:\n        raise ValueError(\"negative duration\")\n    minutes, seconds = divmod(total_seconds, 60)\n    return f\"{minutes:02d}:{seconds:02d}\"", "def formatar_duracao(total_segundos):\n    \"\"\"Retorne uma duração no formato MM:SS.\n\n    Args:\n        total_segundos: Quantidade inteira e não negativa de segundos.\n\n    Returns:\n        Uma string como \"03:07\".\n\n    Raises:\n        ValueError: Se total_segundos for negativo.\n    \"\"\"\n    if total_segundos < 0:\n        raise ValueError(\"negative duration\")\n    minutos, segundos = divmod(total_segundos, 60)\n    return f\"{minutos:02d}:{segundos:02d}\""),
      heading("⚠️ Common errors — comments are not docstrings and stale promises are bugs", "⚠️ Erros comuns — comentários não são docstrings e promessas desatualizadas são bugs"),
      warning("Placing # documentation before def does not populate function.__doc__. Putting the triple-quoted string after an assignment also fails because only the first statement becomes the docstring. A docstring that says “returns int” while code returns str is a contract defect. Copying the same vague text to several functions hides differences instead of documenting them. Syntax errors inside examples also damage trust because readers often copy them directly.", "Colocar documentação com # antes de def não preenche function.__doc__. Colocar string com aspas triplas depois de uma atribuição também falha porque só a primeira instrução vira docstring. Docstring que diz “retorna int” enquanto código retorna str é defeito de contrato. Copiar o mesmo texto vago para várias funções esconde diferenças. Erros de sintaxe nos exemplos prejudicam confiança porque leitores costumam copiá-los."),
      text("Do not document obvious mechanics such as “set total to zero, loop over items, add item”. That explanation becomes stale when implementation changes and does not help a caller. Document why empty data is rejected, what units are expected, whether order is preserved and what the returned object represents. Implementation comments and public docstrings serve different audiences.", "Não documente mecânica óbvia como “defina total como zero, percorra itens, some item”. Essa explicação fica desatualizada quando a implementação muda e não ajuda quem chama. Documente por que dados vazios são rejeitados, quais unidades são esperadas, se a ordem é preservada e o que o objeto retornado representa. Comentários internos e docstrings públicas atendem públicos diferentes."),
      heading("💡 Pro tip — test the documentation contract, not only the calculation", "💡 Dica pro — teste o contrato documental, não apenas o cálculo"),
      tip("For important public functions, add a lightweight test that __doc__ exists and includes essential sections or phrases. This does not prove the prose is perfect, but it prevents documentation from disappearing during refactoring. Pair that structural test with behavior tests so documentation and code remain aligned. Reviewers should read the signature, docstring and tests together as one public interface.", "Para funções públicas importantes, adicione teste leve verificando que __doc__ existe e inclui seções ou frases essenciais. Isso não prova que o texto é perfeito, mas evita desaparecimento durante refatoração. Combine o teste estrutural com testes de comportamento para manter documentação e código alinhados. Revisores devem ler assinatura, docstring e testes como uma única interface pública."),
      heading("📋 Recap and bridge to variable scope", "📋 Resumo e ponte para escopo de variáveis"),
      text("You can now write a one-line summary, document parameters, return values, errors and examples, inspect documentation through __doc__ and help(), and identify stale or implementation-focused prose. The next phase explains where parameter names and local variables live while the function executes. That knowledge prevents accidental dependence on global state and makes the documented contract true in practice.", "Agora você consegue escrever resumo de uma linha, documentar parâmetros, retornos, erros e exemplos, inspecionar documentação com __doc__ e help(), e identificar texto desatualizado ou focado na implementação. A próxima fase explica onde parâmetros e variáveis locais vivem durante a execução. Esse conhecimento evita dependência acidental de estado global e torna o contrato documentado verdadeiro na prática."),
    ],
  },
  exercises: [
    exercise(
      "p15-guided-doc",
      b("🟢 Guided — add a real docstring", "🟢 Guiado — adicione uma docstring real"),
      b("Complete the first statement of normalize_code with a docstring containing the phrase \"Return a normalized code\".", "Complete a primeira instrução de normalize_code com docstring contendo a frase \"Return a normalized code\"."),
      "def normalize_code(value):\n    ___\n    return value.strip().upper()",
      b("True\nAB-10", "True\nAB-10"),
      [b("Use triple quotes inside the function.", "Use aspas triplas dentro da função."), b("The docstring must be the first statement.", "A docstring deve ser a primeira instrução."), b("Keep the exact English phrase requested.", "Mantenha a frase exata em inglês solicitada.")],
      [{ kind: "function", value: "normalize_code" }, { kind: "node", value: "Expr" }, { kind: "node", value: "Return" }],
      exactTest("p15-doc-visible", b("Stores the requested docstring and normalizes", "Armazena docstring e normaliza"), "print(\"Return a normalized code\" in (normalize_code.__doc__ or \"\"))\nprint(normalize_code(\" ab-10 \"))", b("True\nAB-10", "True\nAB-10"), 60, [{ kind: "function", value: "normalize_code" }, { kind: "node", value: "Expr" }, { kind: "node", value: "Return" }], false),
      exactTest("p15-doc-accented", b("Works with accented text", "Funciona com texto acentuado"), "print(normalize_code(\" café \"))", b("CAFÉ", "CAFÉ"), 40, [{ kind: "function", value: "normalize_code" }, { kind: "node", value: "Return" }], true),
      "guided",
      b("Place a docstring where Python and tools can read it.", "Colocar docstring onde Python e ferramentas conseguem ler."),
      b("Public helpers used by imports, forms and APIs.", "Helpers públicos usados por importações, formulários e APIs."),
      { en: ["Docstring is first statement", "Phrase is present", "Function behavior remains correct"], pt: ["Docstring é primeira instrução", "Frase está presente", "Comportamento continua correto"] },
      { en: ["Using a # comment", "Putting docstring after return", "Changing the requested phrase"], pt: ["Usar comentário #", "Colocar docstring após return", "Mudar frase solicitada"] },
    ),
    exercise(
      "p15-complete-contract",
      b("🟡 Complete — document parameters and returns", "🟡 Complete — documente parâmetros e retorno"),
      b("Complete rectangle_area. Its docstring must include Args: and Returns:, and the function must reject negative dimensions.", "Complete rectangle_area. A docstring precisa incluir Args: e Returns:, e a função deve rejeitar dimensões negativas."),
      "def rectangle_area(width, height):\n    \"\"\"Return the area of a rectangle.\n\n    ___:\n        width: Non-negative width.\n        height: Non-negative height.\n\n    ___:\n        The numeric area.\n    \"\"\"\n    if width < 0 or height < 0:\n        raise ValueError(\"negative dimension\")\n    return width * height",
      b("True\n12", "True\n12"),
      [b("The missing headings are Args and Returns.", "Os títulos ausentes são Args e Returns."), b("Keep each heading followed by a colon.", "Mantenha cada título seguido de dois-pontos."), b("Do not remove validation.", "Não remova a validação.")],
      [{ kind: "function", value: "rectangle_area" }, { kind: "node", value: "Expr" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p15-contract-visible", b("Documents sections and calculates area", "Documenta seções e calcula área"), "doc = rectangle_area.__doc__ or \"\"\nprint(\"Args:\" in doc and \"Returns:\" in doc)\nprint(rectangle_area(3, 4))", b("True\n12", "True\n12"), 60, [{ kind: "function", value: "rectangle_area" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }], false),
      exactTest("p15-contract-negative", b("Rejects a negative dimension", "Rejeita dimensão negativa"), "try:\n    rectangle_area(-1, 4)\nexcept ValueError as error:\n    print(error)", b("negative dimension", "negative dimension"), 40, [{ kind: "function", value: "rectangle_area" }, { kind: "node", value: "Raise" }], true),
      "independent",
      b("Document meaning and constraints rather than repeat names.", "Documentar significado e restrições em vez de repetir nomes."),
      b("Utility libraries shared between reports and applications.", "Bibliotecas utilitárias compartilhadas entre relatórios e aplicações."),
      { en: ["Args and Returns exist", "Validation matches documentation", "Area is returned"], pt: ["Args e Returns existem", "Validação corresponde à documentação", "Área é retornada"] },
      { en: ["Misspelling section names", "Removing the docstring quotes", "Documenting negative values as valid"], pt: ["Errar nomes das seções", "Remover aspas da docstring", "Documentar negativos como válidos"] },
    ),
    exercise(
      "p15-zero-errors",
      b("🔴 From scratch — document an error contract", "🔴 Do zero — documente um contrato de erro"),
      b("Write average_score(scores) with a complete docstring containing \"Raises:\" and \"empty scores\". Raise ValueError(\"empty scores\") for [], otherwise return the rounded mean.", "Escreva average_score(scores) com docstring completa contendo \"Raises:\" e \"empty scores\". Gere ValueError(\"empty scores\") para [], senão retorne média arredondada."),
      "def average_score(scores):\n    pass",
      b("True\n80.0", "True\n80.0"),
      [b("Write the docstring before validation.", "Escreva docstring antes da validação."), b("Use if not scores.", "Use if not scores."), b("Use sum(scores) / len(scores).", "Use sum(scores) / len(scores).")],
      [{ kind: "function", value: "average_score" }, { kind: "node", value: "Expr" }, { kind: "node", value: "If" }, { kind: "node", value: "Raise" }, { kind: "call", value: "sum" }, { kind: "call", value: "len" }, { kind: "node", value: "Return" }],
      exactTest("p15-average-visible", b("Documents and calculates an average", "Documenta e calcula média"), "doc = average_score.__doc__ or \"\"\nprint(\"Raises:\" in doc and \"empty scores\" in doc)\nprint(average_score([70, 80, 90]))", b("True\n80.0", "True\n80.0"), 60, [{ kind: "function", value: "average_score" }, { kind: "node", value: "Expr" }, { kind: "call", value: "sum" }, { kind: "node", value: "Return" }], false),
      exactTest("p15-average-empty", b("Raises the documented empty error", "Gera o erro vazio documentado"), "try:\n    average_score([])\nexcept ValueError as error:\n    print(error)", b("empty scores", "empty scores"), 40, [{ kind: "function", value: "average_score" }, { kind: "node", value: "Raise" }], true),
      "independent",
      b("Keep documented errors aligned with runtime behavior.", "Manter erros documentados alinhados ao comportamento."),
      b("Analytics functions that require at least one observation.", "Funções analíticas que exigem pelo menos uma observação."),
      { en: ["Docstring names the error", "Empty input raises exact message", "Mean is returned"], pt: ["Docstring nomeia o erro", "Entrada vazia gera mensagem exata", "Média é retornada"] },
      { en: ["Returning 0 for empty without documenting it", "Printing the mean", "Writing the docstring outside the function"], pt: ["Retornar 0 para vazio sem documentar", "Imprimir média", "Escrever docstring fora da função"] },
    ),
    exercise(
      "p15-transfer",
      b("🟣 Challenge — contract plus executable example", "🟣 Desafio — contrato mais exemplo executável"),
      b("Write format_distance(km) with a docstring containing the exact example line format_distance(1.5) and return values like \"1.50 km\". Reject negative km.", "Escreva format_distance(km) com docstring contendo a linha exata format_distance(1.5) e retorne valores como \"1.50 km\". Rejeite km negativo."),
      "def format_distance(km):\n    pass",
      b("True\n1.50 km", "True\n1.50 km"),
      [b("Include an Example: section.", "Inclua seção Example:."), b("The exact call text must appear in __doc__.", "O texto exato da chamada deve aparecer em __doc__."), b("Use f\"{km:.2f} km\".", "Use f\"{km:.2f} km\".")],
      [{ kind: "function", value: "format_distance" }, { kind: "node", value: "Expr" }, { kind: "node", value: "Raise" }, { kind: "node", value: "JoinedStr" }, { kind: "node", value: "Return" }],
      exactTest("p15-distance-visible", b("Includes example and formats distance", "Inclui exemplo e formata distância"), "print(\"format_distance(1.5)\" in (format_distance.__doc__ or \"\"))\nprint(format_distance(1.5))", b("True\n1.50 km", "True\n1.50 km"), 60, [{ kind: "function", value: "format_distance" }, { kind: "node", value: "Expr" }, { kind: "node", value: "JoinedStr" }, { kind: "node", value: "Return" }], false),
      exactTest("p15-distance-large", b("Formats a large distance", "Formata distância grande"), "print(format_distance(1000000))", b("1000000.00 km", "1000000.00 km"), 40, [{ kind: "function", value: "format_distance" }, { kind: "node", value: "JoinedStr" }, { kind: "node", value: "Return" }], true),
      "challenge",
      b("Use a concrete example to protect an exact formatting contract.", "Usar exemplo concreto para proteger contrato exato de formatação."),
      b("Formatting helpers used by maps, dashboards and exports.", "Helpers de formatação usados por mapas, painéis e exportações."),
      { en: ["Example appears in docstring", "Negative values rejected", "Two decimal places returned"], pt: ["Exemplo aparece na docstring", "Negativos rejeitados", "Duas casas retornadas"] },
      { en: ["Example differs from real output", "No validation", "Using print in the function"], pt: ["Exemplo diferente da saída real", "Sem validação", "Usar print na função"] },
    ),
  ],
  quiz: [
    { id: "q15-v11-1", question: b("Where must a function docstring appear?", "Onde a docstring da função deve aparecer?"), options: [b("Anywhere in the file", "Em qualquer lugar do arquivo"), b("After the first return", "Depois do primeiro return"), b("As the first statement inside the function", "Como primeira instrução dentro da função"), b("Only in README.md", "Apenas no README.md")], correctIndex: 2, explanation: b("Only the first string statement becomes the function __doc__.", "Apenas a primeira instrução string vira o __doc__ da função.") },
    { id: "q15-v11-2", question: b("What should a parameter description add?", "O que a descrição do parâmetro deve acrescentar?"), options: [b("Meaning, units or constraints", "Significado, unidades ou restrições"), b("A copy of the parameter name", "Uma cópia do nome"), b("Every implementation line", "Cada linha da implementação"), b("The author password", "A senha do autor")], correctIndex: 0, explanation: b("Useful documentation reduces uncertainty that names alone cannot remove.", "Documentação útil reduz incerteza que nomes sozinhos não removem.") },
    { id: "q15-v11-3", question: b("Why is stale documentation dangerous?", "Por que documentação desatualizada é perigosa?"), options: [b("It always causes SyntaxError", "Ela sempre causa SyntaxError"), b("It makes code run twice", "Ela faz código rodar duas vezes"), b("It deletes tests", "Ela apaga testes"), b("It gives callers a false contract", "Ela fornece contrato falso a quem chama")], correctIndex: 3, explanation: b("Callers may build correct-looking code based on behavior the function no longer provides.", "Chamadores podem construir código aparentemente correto baseado em comportamento que a função não oferece mais.") },
    { id: "q15-v11-4", question: b("What does help(function_name) use?", "O que help(function_name) utiliza?"), options: [b("Only variable names", "Apenas nomes de variáveis"), b("The function docstring and signature", "A docstring e a assinatura"), b("Git history only", "Apenas histórico Git"), b("Console output", "Saída do console")], correctIndex: 1, explanation: b("Python exposes the stored documentation together with the callable signature.", "Python expõe a documentação armazenada junto da assinatura chamável.") },
  ],
  exam: {
    title: b("Documented inventory helper", "Helper de estoque documentado"),
    scenario: b("Build stock_status(quantity, reorder_at=5). The docstring must describe Args:, Returns: and Raises:, including the exact phrase \"negative quantity\". Return \"reorder\" when quantity <= reorder_at and \"ok\" otherwise.", "Crie stock_status(quantity, reorder_at=5). A docstring deve descrever Args:, Returns: e Raises:, incluindo a frase exata \"negative quantity\". Retorne \"reorder\" quando quantity <= reorder_at e \"ok\" caso contrário."),
    requirements: { en: ["Docstring is the first statement", "Docstring contains Args:, Returns:, Raises: and negative quantity", "Raise ValueError(\"negative quantity\") for negative quantity or reorder_at", "Default reorder_at is 5", "Return only \"reorder\" or \"ok\""], pt: ["Docstring é a primeira instrução", "Docstring contém Args:, Returns:, Raises: e negative quantity", "Gere ValueError(\"negative quantity\") para quantity ou reorder_at negativo", "Padrão reorder_at é 5", "Retorne apenas \"reorder\" ou \"ok\""] },
    starterCode: "def stock_status(quantity, reorder_at=5):\n    pass",
    expectedOutput: b("True\nreorder\nok", "True\nreorder\nok"),
    testCases: [
      exactTest("p15-exam-visible", b("Documents the contract and applies the default", "Documenta contrato e aplica padrão"), "doc = stock_status.__doc__ or \"\"\nprint(all(part in doc for part in [\"Args:\", \"Returns:\", \"Raises:\", \"negative quantity\"]))\nprint(stock_status(5))\nprint(stock_status(6))", b("True\nreorder\nok", "True\nreorder\nok"), 60, [{ kind: "function", value: "stock_status" }, { kind: "node", value: "Expr" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], false),
      exactTest("p15-exam-custom", b("Supports a custom reorder threshold", "Aceita limite personalizado"), "print(stock_status(10, reorder_at=10))", b("reorder", "reorder"), 20, [{ kind: "function", value: "stock_status" }, { kind: "node", value: "If" }, { kind: "node", value: "Return" }], true),
      exactTest("p15-exam-hidden-hardening", b("Raises the documented error", "Gera o erro documentado"), "try:\n    stock_status(-1)\nexcept ValueError as error:\n    print(error)", b("negative quantity", "negative quantity"), 20, [{ kind: "function", value: "stock_status" }, { kind: "node", value: "Raise" }], true),
    ],
  },
}

export const phase16: Phase = {
  id: 16,
  title: b("Variable Scope", "Escopo de Variáveis"),
  description: b("Understand local, enclosing and global names, then keep functions independent through parameters and returns.", "Entenda nomes locais, externos e globais, mantendo funções independentes com parâmetros e retornos."),
  icon: "🔭",
  libraries: [],
  track: 'core',
  stage: 'base',
  estimatedHours: 7,
  lesson: {
    title: b("Scope: where each name exists during execution", "Escopo: onde cada nome existe durante a execução"),
    blocks: [
      heading("🌍 World hook — scope prevents unrelated parts of a system from overwriting each other", "🌍 Gancho do mundo real — escopo evita que partes sem relação sobrescrevam umas às outras"),
      text("Large applications use the same ordinary names thousands of times: total, item, user, result and status. If every assignment changed one shared global variable, a report could corrupt a checkout and a game timer could change a network retry count. Scope gives each function a private local namespace for its parameters and temporary values. That isolation is one reason teams can build many features in the same codebase.", "Aplicações grandes usam os mesmos nomes comuns milhares de vezes: total, item, user, result e status. Se toda atribuição alterasse uma variável global compartilhada, relatório poderia corromper checkout e timer de jogo poderia mudar tentativas de rede. Escopo dá a cada função um namespace local privado para parâmetros e temporários. Esse isolamento é um motivo pelo qual equipes constroem muitas funcionalidades na mesma base."),
      text("Python resolves names using the LEGB idea: Local, Enclosing, Global and Built-in. At this level, focus on local and global first. A parameter is local to its function call. An assignment inside a function normally creates or changes a local name. Reading a name that is not local may find a global value, but assigning to that same name changes the rules and can cause UnboundLocalError.", "Python resolve nomes pela ideia LEGB: Local, Enclosing, Global e Built-in. Neste nível, foque primeiro em local e global. Um parâmetro é local à chamada. Uma atribuição dentro de função normalmente cria ou altera nome local. Ler nome que não é local pode encontrar valor global, mas atribuir ao mesmo nome muda as regras e pode causar UnboundLocalError."),
      heading("🧩 Physical analogy — labeled rooms in an office building", "🧩 Analogia física — salas identificadas num prédio"),
      text("Think of every function call as a temporary meeting room. Notes written on that room whiteboard are local variables. Another room may also write “total” without conflict because the boards are separate. The lobby notice board is global: everyone can read it, but letting every room erase and rewrite it creates coordination problems. Parameters are documents brought into the room; return values are the approved result carried back out.", "Pense em cada chamada como sala de reunião temporária. Notas no quadro são variáveis locais. Outra sala também pode escrever “total” sem conflito porque os quadros são separados. O quadro do saguão é global: todos podem ler, mas permitir que toda sala apague e reescreva cria problemas. Parâmetros são documentos levados para dentro; retornos são o resultado aprovado levado para fora."),
      text("When the meeting ends, ordinary local notes disappear. If a returned object is stored by the caller, that object continues to exist outside, but the local variable name itself is gone. This distinction between names and objects becomes increasingly important with lists, dictionaries and classes. Scope controls where a name can be resolved; mutability controls whether an object can be changed.", "Quando a reunião termina, notas locais comuns desaparecem. Se um objeto retornado é armazenado por quem chamou, o objeto continua existindo, mas o nome local acabou. Essa distinção entre nomes e objetos fica mais importante com listas, dicionários e classes. Escopo controla onde um nome é resolvido; mutabilidade controla se um objeto pode ser alterado."),
      heading("🐍 Fundamentals 1 — local names are created per call", "🐍 Fundamentos 1 — nomes locais são criados por chamada"),
      code("def add_tax(amount):\n    rate = 0.13       # local name\n    total = amount * (1 + rate)\n    return round(total, 2)\n\nprint(add_tax(100))\nprint(add_tax(50))\n# rate and total are not available here", "def adicionar_imposto(valor):\n    taxa = 0.13       # nome local\n    total = valor * (1 + taxa)\n    return round(total, 2)\n\nprint(adicionar_imposto(100))\nprint(adicionar_imposto(50))\n# taxa e total não existem aqui"),
      text("Each call gets fresh bindings for amount, rate and total. The second call cannot see values from the first. Trying to print rate outside the function raises NameError because the global scope has no such name. This is useful protection: implementation details stay inside the function unless they are deliberately returned.", "Cada chamada recebe ligações novas para amount, rate e total. A segunda não vê valores da primeira. Tentar imprimir rate fora gera NameError porque o escopo global não possui esse nome. Isso é proteção útil: detalhes de implementação ficam dentro, salvo quando retornados deliberadamente."),
      heading("🐍 Fundamentals 2 — shadowing does not change the outer name", "🐍 Fundamentos 2 — sombreamento não altera o nome externo"),
      code("status = \"global\"\n\ndef show_status():\n    status = \"local\"\n    return status\n\nprint(show_status())  # local\nprint(status)         # global", "status = \"global\"\n\ndef mostrar_status():\n    status = \"local\"\n    return status\n\nprint(mostrar_status())  # local\nprint(status)            # global"),
      text("The local assignment shadows the global name while the function runs. It does not edit the global string. Shadowing is legal but can confuse readers when the two meanings are unrelated. Prefer specific names such as global_status and display_status, or avoid the global value entirely by passing it as a parameter.", "A atribuição local sombreia o nome global enquanto a função roda. Ela não edita a string global. Sombreamento é permitido, mas pode confundir quando os significados não têm relação. Prefira nomes específicos como global_status e display_status, ou evite o global passando o valor como parâmetro."),
      heading("🐍 Fundamentals 3 — why UnboundLocalError happens", "🐍 Fundamentos 3 — por que UnboundLocalError acontece"),
      code("total = 10\n\ndef broken_add(amount):\n    total = total + amount  # UnboundLocalError\n    return total\n\n# Because assignment makes total local for the entire function,\n# Python tries to read that local name before it has a value.", "total = 10\n\ndef soma_quebrada(valor):\n    total = total + valor  # UnboundLocalError\n    return total\n\n# Como a atribuição torna total local em toda a função,\n# Python tenta ler esse nome local antes de ele receber valor."),
      text("Python determines local names when compiling the function body. Because total is assigned anywhere in broken_add, every reference to total in that function is treated as local unless global or nonlocal says otherwise. The clean fix is usually not global. Pass the current total into the function and return the updated value, making the state transition explicit and testable.", "Python determina nomes locais ao compilar o corpo. Como total recebe atribuição em algum ponto de broken_add, toda referência a total nessa função é tratada como local, salvo global ou nonlocal. A correção limpa normalmente não é global. Passe o total atual e retorne o atualizado, tornando a transição explícita e testável."),
      heading("🐍 Fundamentals 4 — parameters and return replace hidden global mutation", "🐍 Fundamentos 4 — parâmetros e retorno substituem mutação global oculta"),
      code("def add_amount(current_total, amount):\n    if amount < 0:\n        raise ValueError(\"negative amount\")\n    return current_total + amount\n\ntotal = 0\ntotal = add_amount(total, 25)\ntotal = add_amount(total, 40)\nprint(total)", "def adicionar_valor(total_atual, valor):\n    if valor < 0:\n        raise ValueError(\"negative amount\")\n    return total_atual + valor\n\ntotal = 0\ntotal = adicionar_valor(total, 25)\ntotal = adicionar_valor(total, 40)\nprint(total)"),
      text("The caller now owns the state and every update is visible. Tests can call add_amount with any current total without resetting a module global. The function can run in parallel tasks, notebooks or web requests without sharing accidental state. Explicit data flow is a professional habit that pays off long before advanced concurrency appears.", "Agora quem chama é dono do estado e toda atualização é visível. Testes podem chamar add_amount com qualquer total sem redefinir global do módulo. A função pode rodar em tarefas paralelas, notebooks ou requisições web sem compartilhar estado acidental. Fluxo explícito é hábito profissional que ajuda muito antes de concorrência avançada aparecer."),
      heading("🐍 Fundamentals 5 — enclosing scope and a simple closure", "🐍 Fundamentos 5 — escopo externo e closure simples"),
      code("def make_multiplier(factor):\n    if factor < 0:\n        raise ValueError(\"negative factor\")\n\n    def multiply(value):\n        return value * factor  # factor comes from enclosing scope\n\n    return multiply\n\ndouble = make_multiplier(2)\nprint(double(7))", "def criar_multiplicador(fator):\n    if fator < 0:\n        raise ValueError(\"negative factor\")\n\n    def multiplicar(valor):\n        return valor * fator  # fator vem do escopo externo\n\n    return multiplicar\n\ndobrar = criar_multiplicador(2)\nprint(dobrar(7))"),
      text("The inner function remembers factor from the enclosing call even after make_multiplier returns. This is a closure. You do not need closures for most beginner tasks, but seeing one clarifies the E in LEGB and shows that local data can be deliberately preserved without becoming global. Later decorators and callbacks build on this behavior.", "A função interna lembra factor da chamada externa mesmo depois de make_multiplier retornar. Isso é closure. Você não precisa de closures na maioria das tarefas iniciais, mas vê-la esclarece o E de LEGB e mostra que dado local pode ser preservado deliberadamente sem virar global. Decorators e callbacks usam esse comportamento depois."),
      heading("🏗️ Real scenario 1 — personal budget updates", "🏗️ Cenário real 1 — atualizações de orçamento pessoal"),
      text("A budgeting app receives a current balance and one transaction. A pure update function returns the next balance rather than modifying a module variable. This lets the app replay transactions, undo a change or compare two scenarios because each step is an explicit input and output.", "Um app de orçamento recebe saldo atual e uma transação. Uma função pura retorna o próximo saldo em vez de modificar variável do módulo. Isso permite reproduzir transações, desfazer mudança ou comparar cenários porque cada etapa é entrada e saída explícitas."),
      code("def apply_transaction(balance, amount, kind):\n    if amount < 0:\n        raise ValueError(\"negative transaction\")\n    if kind == \"income\":\n        return balance + amount\n    if kind == \"expense\":\n        return balance - amount\n    raise ValueError(\"unknown transaction kind\")\n\nbalance = 1000\nfor amount, kind in [(200, \"income\"), (75, \"expense\")]:\n    balance = apply_transaction(balance, amount, kind)\nprint(balance)", "def aplicar_transacao(saldo, valor, tipo):\n    if valor < 0:\n        raise ValueError(\"negative transaction\")\n    if tipo == \"income\":\n        return saldo + valor\n    if tipo == \"expense\":\n        return saldo - valor\n    raise ValueError(\"unknown transaction kind\")\n\nsaldo = 1000\nfor valor, tipo in [(200, \"income\"), (75, \"expense\")]:\n    saldo = aplicar_transacao(saldo, valor, tipo)\nprint(saldo)"),
      text("Because balance is reassigned in the outer workflow, the state transition is visible. apply_transaction itself has no hidden memory. This distinction makes it easy to test a negative amount, an unknown type and a very large balance. It also allows a future interface to store each returned balance in a history list.", "Como balance é reatribuído no fluxo externo, a transição fica visível. apply_transaction não possui memória escondida. Essa distinção facilita testar valor negativo, tipo desconhecido e saldo muito grande. Também permite que interface futura armazene cada saldo retornado num histórico."),
      heading("🏗️ Real scenario 2 — game score factory", "🏗️ Cenário real 2 — fábrica de pontuação de jogo"),
      text("A game applies different score multipliers for difficulty levels. A closure can capture one validated multiplier and return a focused scoring function. The multiplier is not global, so creating a hard-mode scorer does not change an easy-mode scorer. Each returned function keeps its own enclosing value.", "Um jogo aplica multiplicadores diferentes por dificuldade. Uma closure pode capturar um multiplicador validado e retornar função focada de pontuação. O multiplicador não é global, então criar pontuação difícil não altera a fácil. Cada função retornada mantém seu próprio valor externo."),
      code("def score_rule(multiplier):\n    if multiplier <= 0:\n        raise ValueError(\"invalid multiplier\")\n\n    def calculate(base_points):\n        if base_points < 0:\n            raise ValueError(\"negative points\")\n        return base_points * multiplier\n\n    return calculate\n\neasy_score = score_rule(1)\nhard_score = score_rule(3)\nprint(easy_score(100), hard_score(100))", "def regra_pontuacao(multiplicador):\n    if multiplicador <= 0:\n        raise ValueError(\"invalid multiplier\")\n\n    def calcular(pontos_base):\n        if pontos_base < 0:\n            raise ValueError(\"negative points\")\n        return pontos_base * multiplicador\n\n    return calcular\n\npontuar_facil = regra_pontuacao(1)\npontuar_dificil = regra_pontuacao(3)\nprint(pontuar_facil(100), pontuar_dificil(100))"),
      heading("⚠️ Common errors — NameError, UnboundLocalError and hidden global state", "⚠️ Erros comuns — NameError, UnboundLocalError e estado global escondido"),
      warning("NameError means Python could not resolve a name in available scopes. UnboundLocalError means a name was classified as local but read before receiving a value. Using global may silence that error, but it also creates shared mutation that tests and concurrent workflows must reset. Another common mistake is assuming a local assignment changes a same-named global; it only shadows it. Finally, mutating a list parameter changes the object owned by the caller even though the parameter name is local.", "NameError significa que Python não resolveu nome nos escopos disponíveis. UnboundLocalError significa nome classificado como local, mas lido antes de receber valor. Usar global pode silenciar o erro, porém cria mutação compartilhada que testes e fluxos concorrentes precisam redefinir. Outro erro é assumir que atribuição local muda global de mesmo nome; ela apenas sombreia. Por fim, alterar lista parâmetro muda objeto do chamador mesmo que o nome seja local."),
      text("Scope and mutability are related but not identical. A local parameter can refer to a global list object passed by the caller. Calling append mutates that shared object. Rebinding the local parameter to a new list does not change the caller’s variable. When a function should not mutate input, create a copy or return a new collection and document that promise.", "Escopo e mutabilidade se relacionam, mas não são iguais. Um parâmetro local pode referenciar lista global passada pelo chamador. Chamar append altera o objeto compartilhado. Religar o parâmetro local a nova lista não muda a variável do chamador. Quando a função não deve alterar entrada, crie cópia ou retorne coleção nova e documente a promessa."),
      heading("💡 Pro tip — make state ownership visible", "💡 Dica pro — deixe a propriedade do estado visível"),
      tip("Ask “who owns this value?” If one workflow owns it, keep the variable in that workflow and pass it into helper functions. If several operations need coordinated state, a class or dedicated object later may be clearer than globals. Use global only for deliberate module-level constants or rare controlled state, not as the default solution to an UnboundLocalError.", "Pergunte “quem é dono deste valor?”. Se um fluxo é dono, mantenha a variável nele e passe aos helpers. Se várias operações precisam de estado coordenado, uma classe ou objeto dedicado depois pode ser mais claro que globals. Use global apenas para constantes deliberadas ou raro estado controlado, não como solução padrão para UnboundLocalError."),
      heading("📋 Recap and bridge to modules", "📋 Resumo e ponte para módulos"),
      text("You can now identify local and global names, explain shadowing and UnboundLocalError, replace hidden mutation with parameter-plus-return flow, recognize a simple enclosing scope and distinguish local names from mutable objects. The next steps organize related functions into modules, where module scope becomes another explicit boundary for names, imports and reusable behavior.", "Agora você consegue identificar nomes locais e globais, explicar sombreamento e UnboundLocalError, substituir mutação escondida por parâmetro mais retorno, reconhecer escopo externo simples e distinguir nomes locais de objetos mutáveis. Os próximos passos organizam funções relacionadas em módulos, onde escopo de módulo vira outra fronteira explícita para nomes, imports e comportamento reutilizável."),
    ],
  },
  exercises: [
    exercise(
      "p16-guided-update",
      b("🟢 Guided — update state without a global", "🟢 Guiado — atualize estado sem global"),
      b("Complete add_points(current, earned). Reject negative earned points and return the new total.", "Complete add_points(current, earned). Rejeite pontos earned negativos e retorne o novo total."),
      "def add_points(current, earned):\n    if earned < 0:\n        raise ValueError(\"negative points\")\n    new_total = ___ + ___\n    return ___",
      b("125", "125"),
      [b("Use both parameters in the addition.", "Use ambos os parâmetros na soma."), b("Return new_total.", "Retorne new_total."), b("Do not create or edit a global score.", "Não crie nem altere score global.")],
      [{ kind: "function", value: "add_points" }, { kind: "node", value: "BinOp" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p16-update-visible", b("Returns an updated score", "Retorna pontuação atualizada"), "print(add_points(100, 25))", b("125", "125"), 60, [{ kind: "function", value: "add_points" }, { kind: "node", value: "BinOp" }, { kind: "node", value: "Return" }], false),
      exactTest("p16-update-large", b("Handles a large current score", "Trata pontuação atual grande"), "print(add_points(1000000, 5))", b("1000005", "1000005"), 40, [{ kind: "function", value: "add_points" }, { kind: "node", value: "BinOp" }, { kind: "node", value: "Return" }], true),
      "guided",
      b("Make a state transition explicit through parameters and return.", "Tornar transição de estado explícita por parâmetros e retorno."),
      b("Scores, balances, inventory and progress counters.", "Pontuações, saldos, estoque e contadores de progresso."),
      { en: ["No global state", "Rejects negative earned value", "Returns new total"], pt: ["Sem estado global", "Rejeita earned negativo", "Retorna novo total"] },
      { en: ["Using global", "Printing the total", "Changing only current locally"], pt: ["Usar global", "Imprimir total", "Mudar apenas current localmente"] },
    ),
    exercise(
      "p16-complete-shadow",
      b("🟡 Complete — understand shadowing", "🟡 Complete — entenda sombreamento"),
      b("Finish local_status so it creates and returns a local uppercase value without changing the global status.", "Termine local_status para criar e retornar valor local em maiúsculas sem mudar o status global."),
      "status = \"global\"\n\ndef local_status(value):\n    status = value.___()\n    return ___",
      b("READY\nglobal", "READY\nglobal"),
      [b("Call upper on value.", "Chame upper em value."), b("Return the local status.", "Retorne status local."), b("Do not use global status.", "Não use global status.")],
      [{ kind: "function", value: "local_status" }, { kind: "call", value: "upper" }, { kind: "node", value: "Return" }],
      exactTest("p16-shadow-visible", b("Returns local value and preserves global", "Retorna local e preserva global"), "print(local_status(\"ready\"))\nprint(status)", b("READY\nglobal", "READY\nglobal"), 60, [{ kind: "function", value: "local_status" }, { kind: "call", value: "upper" }, { kind: "node", value: "Return" }], false),
      exactTest("p16-shadow-accented", b("Handles accented local text", "Trata texto local acentuado"), "print(local_status(\"ação\"))\nprint(status)", b("AÇÃO\nglobal", "AÇÃO\nglobal"), 40, [{ kind: "function", value: "local_status" }, { kind: "call", value: "upper" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Observe that a local assignment shadows rather than edits a global.", "Observar que atribuição local sombreia em vez de editar global."),
      b("Formatting temporary labels without changing application configuration.", "Formatação de rótulos temporários sem mudar configuração do app."),
      { en: ["Local uppercase returned", "Global stays unchanged", "No global statement"], pt: ["Maiúsculo local retornado", "Global permanece", "Sem instrução global"] },
      { en: ["Using global status", "Returning the global before assignment", "Changing the outer variable"], pt: ["Usar global status", "Retornar global antes da atribuição", "Alterar variável externa"] },
    ),
    exercise(
      "p16-zero-balance",
      b("🔴 From scratch — process transactions explicitly", "🟢 Do zero — processe transações explicitamente"),
      b("Write apply_changes(start, changes). Each change is numeric. Return start plus all changes, reject a non-numeric change with ValueError(\"invalid change\"), and do not mutate global state.", "Escreva apply_changes(start, changes). Cada mudança é numérica. Retorne start mais todas as mudanças, rejeite não numérica com ValueError(\"invalid change\") e não altere estado global."),
      "def apply_changes(start, changes):\n    pass",
      b("125", "125"),
      [b("Create a local total = start.", "Crie total local = start."), b("Loop through changes and validate each item.", "Percorra changes e valide cada item."), b("Return total after the loop.", "Retorne total após o loop.")],
      [{ kind: "function", value: "apply_changes" }, { kind: "node", value: "For" }, { kind: "node", value: "If" }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return" }],
      exactTest("p16-balance-visible", b("Applies positive and negative changes", "Aplica mudanças positivas e negativas"), "print(apply_changes(100, [50, -25]))", b("125", "125"), 60, [{ kind: "function", value: "apply_changes" }, { kind: "node", value: "For" }, { kind: "node", value: "Return" }], false),
      exactTest("p16-balance-empty", b("Handles an empty change list", "Trata lista de mudanças vazia"), "print(apply_changes(42, []))", b("42", "42"), 40, [{ kind: "function", value: "apply_changes" }, { kind: "node", value: "For" }, { kind: "node", value: "Return" }], true),
      "independent",
      b("Keep accumulation local and return the final state.", "Manter acumulação local e retornar estado final."),
      b("Applying financial, inventory or progress events.", "Aplicação de eventos financeiros, estoque ou progresso."),
      { en: ["Empty input preserves start", "Negative changes are allowed", "Non-numeric values rejected"], pt: ["Vazio preserva start", "Mudanças negativas permitidas", "Não numéricos rejeitados"] },
      { en: ["Using a module total", "Rejecting all negatives", "Returning inside the loop"], pt: ["Usar total de módulo", "Rejeitar todo negativo", "Retornar dentro do loop"] },
    ),
    exercise(
      "p16-transfer",
      b("🟣 Challenge — create a closure", "🟣 Desafio — crie uma closure"),
      b("Write make_prefix(prefix). Strip prefix, reject an empty value, and return an inner function label(value) that returns \"prefix: value\".", "Escreva make_prefix(prefix). Limpe prefix, rejeite vazio e retorne função interna label(value) que devolve \"prefix: value\"."),
      "def make_prefix(prefix):\n    pass",
      b("INFO: started", "INFO: started"),
      [b("Validate and normalize prefix in the outer function.", "Valide e normalize prefix na função externa."), b("Define label(value) inside make_prefix.", "Defina label(value) dentro de make_prefix."), b("Return label without calling it.", "Retorne label sem chamá-la.")],
      [{ kind: "function", value: "make_prefix" }, { kind: "node", value: "FunctionDef", minCount: 2 }, { kind: "node", value: "Raise" }, { kind: "node", value: "Return", minCount: 2 }, { kind: "node", value: "JoinedStr" }],
      exactTest("p16-closure-visible", b("Creates a reusable prefix function", "Cria função de prefixo reutilizável"), "info = make_prefix(\" info \")\nprint(info(\"started\"))", b("INFO: started", "INFO: started"), 60, [{ kind: "function", value: "make_prefix" }, { kind: "node", value: "FunctionDef", minCount: 2 }, { kind: "node", value: "Return", minCount: 2 }], false),
      exactTest("p16-closure-empty", b("Rejects an empty prefix", "Rejeita prefixo vazio"), "try:\n    make_prefix(\"   \")\nexcept ValueError as error:\n    print(error)", b("empty prefix", "empty prefix"), 40, [{ kind: "function", value: "make_prefix" }, { kind: "node", value: "Raise" }], true),
      "challenge",
      b("Use enclosing scope deliberately without global state.", "Usar escopo externo deliberadamente sem estado global."),
      b("Creating configured formatters, validators and callbacks.", "Criação de formatadores, validadores e callbacks configurados."),
      { en: ["Inner function returned", "Prefix remembered", "Empty prefix rejected"], pt: ["Função interna retornada", "Prefixo lembrado", "Vazio rejeitado"] },
      { en: ["Calling label before returning it", "Using a global prefix", "Returning a formatted string instead of a function"], pt: ["Chamar label antes de retornar", "Usar prefixo global", "Retornar string em vez de função"] },
    ),
  ],
  quiz: [
    { id: "q16-v11-1", question: b("Why does total = total + amount inside a function often raise UnboundLocalError?", "Por que total = total + amount dentro de função costuma gerar UnboundLocalError?"), options: [b("Python treats total as local because it is assigned there", "Python trata total como local porque recebe atribuição ali"), b("Addition is forbidden in functions", "Soma é proibida em funções"), b("Global strings cannot be read", "Strings globais não podem ser lidas"), b("Return is missing before assignment", "Falta return antes da atribuição")], correctIndex: 0, explanation: b("The assignment makes every total reference in that function local, so the first read happens before a local value exists.", "A atribuição torna toda referência a total local, então a primeira leitura ocorre antes de existir valor local.") },
    { id: "q16-v11-2", question: b("What is usually cleaner than mutating a global total?", "O que normalmente é mais limpo que alterar total global?"), options: [b("Another global variable", "Outra variável global"), b("Printing more messages", "Imprimir mais mensagens"), b("Pass current total and return the new total", "Passar total atual e retornar o novo"), b("Rename it x", "Renomear para x")], correctIndex: 2, explanation: b("Explicit input and output make state ownership and testing clear.", "Entrada e saída explícitas tornam propriedade do estado e teste claros.") },
    { id: "q16-v11-3", question: b("What does shadowing mean?", "O que significa sombreamento?"), options: [b("Deleting a built-in", "Apagar built-in"), b("A local name temporarily hides an outer name with the same spelling", "Nome local esconde temporariamente nome externo de mesma escrita"), b("Encrypting a variable", "Criptografar variável"), b("Copying all globals", "Copiar todos os globais")], correctIndex: 1, explanation: b("The outer name still exists and is visible again after the local scope ends.", "O nome externo continua existindo e volta a aparecer após o escopo local terminar.") },
    { id: "q16-v11-4", question: b("What does a closure preserve?", "O que uma closure preserva?"), options: [b("Every global variable", "Toda variável global"), b("Console history", "Histórico do console"), b("Only imported modules", "Apenas módulos importados"), b("Values from an enclosing function used by the returned inner function", "Valores da função externa usados pela interna retornada")], correctIndex: 3, explanation: b("The inner function keeps access to referenced enclosing bindings after the outer call ends.", "A função interna mantém acesso às ligações externas referenciadas após a chamada externa terminar.") },
  ],
  exam: {
    title: b("Explicit inventory state flow", "Fluxo explícito de estado de estoque"),
    scenario: b("Build update_stock(current, movements). Each movement is [kind, quantity], where kind is \"in\" or \"out\" and quantity is non-negative. Keep total local, reject unknown kinds or negative quantity, and reject any movement that would make stock negative.", "Crie update_stock(current, movements). Cada movimento é [kind, quantity], onde kind é \"in\" ou \"out\" e quantity não negativo. Mantenha total local, rejeite tipos desconhecidos ou quantidade negativa e rejeite movimento que torne estoque negativo."),
    requirements: { en: ["Define update_stock(current, movements)", "Use a local total initialized from current", "Return the final total", "Raise ValueError(\"invalid movement\") for unknown kind or negative quantity", "Raise ValueError(\"negative stock\") if an out movement would cross below zero", "Do not use global"], pt: ["Defina update_stock(current, movements)", "Use total local iniciado por current", "Retorne total final", "Gere ValueError(\"invalid movement\") para tipo desconhecido ou quantidade negativa", "Gere ValueError(\"negative stock\") se saída deixar abaixo de zero", "Não use global"] },
    starterCode: "def update_stock(current, movements):\n    pass",
    expectedOutput: b("12", "12"),
    testCases: [
      exactTest("p16-exam-visible", b("Applies incoming and outgoing stock", "Aplica entradas e saídas"), "print(update_stock(10, [[\"in\", 5], [\"out\", 3]]))", b("12", "12"), 60, [{ kind: "function", value: "update_stock" }, { kind: "node", value: "For" }, { kind: "node", value: "If", minCount: 2 }, { kind: "node", value: "Return" }], false),
      exactTest("p16-exam-empty", b("Preserves current stock for no movements", "Preserva estoque atual sem movimentos"), "print(update_stock(7, []))", b("7", "7"), 20, [{ kind: "function", value: "update_stock" }, { kind: "node", value: "For" }, { kind: "node", value: "Return" }], true),
      exactTest("p16-exam-hidden-hardening", b("Rejects a movement below zero stock", "Rejeita movimento abaixo de zero"), "try:\n    update_stock(2, [[\"out\", 3]])\nexcept ValueError as error:\n    print(error)", b("negative stock", "negative stock"), 20, [{ kind: "function", value: "update_stock" }, { kind: "node", value: "Raise" }], true),
    ],
  },
}
