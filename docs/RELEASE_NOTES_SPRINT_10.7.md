# Sprint 10.7 — Final local AI capstone and complete portfolio

## Final Phase 68 capstone

The course now ends with **Private Local Document Copilot**, an auditable RAG-core project that runs without an external AI API.

The learner must implement:

- immutable document chunks;
- explicit local embedding records;
- consistent tokenization;
- average-vector construction;
- safe cosine similarity;
- thresholded top-k retrieval;
- extractive grounded answers;
- visible citations;
- honest abstention for unknown or unsupported queries;
- deterministic tests and a main guard.

The browser capstone deliberately implements the trustworthy retrieval core. A local language model can be connected later in the desktop track, but it must never replace evidence, citations or abstention.

## Portfolio consolidation

The portfolio now includes six milestones:

1. Phase 27 — Python foundations;
2. Phase 39 — professional Python;
3. Phase 53 — advanced Python and engineering;
4. Phase 60 — data and Machine Learning;
5. Phase 64 — neural networks and Transformers;
6. Phase 68 — private local AI.

The Phase 64 artifact was previously defined but absent from the portfolio project list. Sprint 10.7 fixes that gap.

A combined Markdown portfolio becomes available only after all six projects are completed through normal progression. It includes the learner's own contract, pseudocode, implementation, test evidence and refactoring decisions for every artifact.

## Progression and privacy

No owner unlock or hidden curriculum bypass was added. Future artifacts stay hidden until their normal milestones become available. The combined portfolio is created locally in the browser and should be reviewed before publication.
