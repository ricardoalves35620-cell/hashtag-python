# Sprint 10.5 — Data and Machine Learning portfolio

Phase 60 now ends with a fourth portfolio integrator: **Claim Risk Classification Pipeline**.

- Defines an explicit TRAIN/TEST dataset contract.
- Validates rows before model fitting.
- Fits min/max preprocessing from training rows only.
- Implements a nearest-centroid classifier without hiding the data flow behind a framework.
- Reports predictions, accuracy, precision and recall with protected zero denominators.
- Tests perfect separation, a false positive, invalid input, empty evaluation and missing training classes.
- Blocks Phase 61 until both the Phase 60 exam and integrator are complete.
- Exports a fourth README portfolio artifact through normal progression.
- Does not expose future milestones or bypass course locking.
